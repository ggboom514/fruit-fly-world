# 果蝇世界

一块覆盖整个桌面的透明画布，上面生活着一群会繁殖、会长大、会死、会烂掉的果蝇�?
## 运行

```bash
bun install          # 如果 node_modules 还在就不用跑
bun start            # 等价�?electron .
```

> 如果 `bun install` 之后 `node_modules/electron/dist/electron.exe` 不存在，
> 说明 postinstall �?bun 拦下、或者从 GitHub 下载太慢。用国内镜像补一次：
>
> ```powershell
> $env:ELECTRON_MIRROR='https://npmmirror.com/mirrors/electron/'
> $env:ELECTRON_CUSTOM_DIR='{{ version }}'
> bun node_modules/electron/install.js
> ```
>
> `ELECTRON_CUSTOM_DIR` 必须�?`{{ version }}`：npm 官方的目录名�?`v` 前缀�?> npmmirror 不带，不覆盖的话�?404�?
### 两个踩过的坑

**`ELECTRON_RUN_AS_NODE=1`**：如果这个环境变量存在，`electron` 会退化成一个纯 Node 进程�?�?`TypeError: Cannot read properties of undefined (reading 'on')`（`require('electron')` 返回的是路径字符串而不�?API）�?这不是代码问题，清掉即可�?
```powershell
Remove-Item Env:ELECTRON_RUN_AS_NODE
```

**控制台里�?CSP 警告**：`index.html` 故意没有�?CSP meta —�?本应用完全离线、也没开
`nodeIntegration`，加 `'self'` 反而会�?`file://` 下把本地 ES 模块一起挡掉�?这条警告只在开发模式出现，打包后不会有�?
## 自检与调�?
```bash
bun run check            # 秒级：只解析不执行，抓语法错误（先跑这个�?bun run selftest         # 加载页面、验�?preload 桥、试画一帧，然后退出（不弹窗）
bun run sim              # 无头模拟 120 分钟，跑完整生命周期
bun run sim 600          # 模拟 600 分钟
```

�?**`check` 要排�?`selftest` 前面�?*，因为它专治一种特别误导人的失败：
`main.js` 里的自检是一整块模板字符串，**里面混进一个反引号�?`${` 就会让整�?main.js 语法错误**。而那种情况下 electron 报的�?`App threw an error during load`�?自检自己�?15 秒超�?*根本没机会注�?*（它�?app ready 之后才跑）—�?于是 `bun run selftest` **永远挂着不退�?*，看起来像卡死，不像编译失败�?
这个坑踩过三次，每次都是在注释里顺手写了个反引号（比如想引用 `e.target`
这种标识符）。`check` 会直接指到出问题的文件和行�?
`selftest` 在改完代码或者打包之后跑一下最省事：它会检�?ES 模块有没有加载成功�?preload 暴露的方法全不全、渲染路径会不会抛异常，然后用退出码告诉你结果（0 = 通过）�?它还会把**存档链路**完整走一遍：序列�?�?IPC �?写盘 �?读盘 �?反序列化，以�?「启动选择框弹�?�?点继�?�?收起 �?放开鼠标」这段接线�?经济系统也在里面：金额格式、商�?/ 出售�?/ 数据面板�?DOM 是否齐全�?六档配色是不是真的切过去了（读的�?`getComputedStyle` 的边框色，不是类名，
�?*面板上那行字写的是不是价值档**一起查）、以及卡片有没有错误地参与鼠标穿透判定�?「查看」工具单独一组：按钮真的点了才切过去、飞行中的虫也点得开�?**罐子压在虫子上时点下去开的是面板而不是拖罐子**（这一条是照着用户报的 bug 写回去的），
以及反过来「观察模式仍然能拖罐子、拿着苍蝇拍则不能」�?数据面板那组还单独钉着**卡片上的两行档位**：价值档那行大字（六档的名字 / 颜色 / 特效
三样一起查）、体格那行小字（轻盈 / 超重 / 巨兽 逐个喂进去看写出来对不对），
以及**幼虫根本点不开**（`_inspectableAt` 返回 null、`_overInspectable()` �?false�?`openInspect(null)` 不开卡，外加一条「成虫和幼虫叠在一起时必须点到成虫」）�?养蝇人配置卡那组里钉着：六档价值档齐全、卡片上**不再有任何输入控�?*�?`world.keeper` �?*没有 `minValue`**（价格滑条已整条删掉），以及自动出售
**真的按档筛�?* —�?摆一只刚羽化的轻蝇和一只顶到最重的老蝇，逐档试过去，
要求轻蝇**只有最低档**卖得掉、重�?*恰好命中一�?*且不是最低那档�?这比「选对了能卖掉」强：后者只说明有个筛子，说明不了筛的是价值档�?投放面板也一样：三行六个按钮、价格文本、钱�?0 时全部置灰、给够钱又全部可用�?旧的 `#btn-food` / `#btn-spawn` 确实已经不在了，以及食物投放区参考框
**按配置摆�?*（真的去�?`config.food.zone.show` 再量 DOM 的位置，
而不是自己重算一遍公式——那样测的是断言自己的算术）且不吞鼠标�?罐中果蝇小窗和捐款卡片也各有一组：小窗「有罐子才出现、默认收起、点标题条真的能开合�?层级高过面板、收起时不占鼠标」，卡片「钉在屏幕正中、层级高过面板�?指针压上去才接管鼠标」—�?*两条层级断言都是�?`getComputedStyle().zIndex`**�?因为漏写那一�?CSS 不会有任何别的症状，只有重叠时才糊�?自检期间读写的是 `save.selftest.json`，不会碰你的真存档�?
> �?量位置的断言**必须在元素还显示着的时候跑**�?> `display: none` 的元�?`getBoundingClientRect()` 返回的是�?0�?> 断言写在关闭之后就变成「拿 0 和屏幕中心比」——只会永远通过�?> 捐款卡片那一段先显式查一�?`card.width > 0`，就是为了堵这个�?
> �?�?*沙箱或某些工具里**直接�?`electron .` 会报 `ipcMain 未定义` —�?> 那是环境变量 `ELECTRON_RUN_AS_NODE=1` �?Electron 退化成普�?Node 了，不是代码问题�?> 清掉它再跑：`Remove-Item Env:ELECTRON_RUN_AS_NODE`�?>
> 同一个变量也会让**打包好的 exe** 出错，但报的是另一副面孔：
> `FruitFlyWorld.exe: bad option: --selftest`（退出码 9）。这�?Node 在拒�?> 它不认识的命令行参数，而不�?Chromium —�?看到 `bad option` 就该想到这个变量�?> `bun run dist` 同理：不清掉的话，`electron-builder` 也起不来�?
`sim` 不启动图形界面，直接�?`World` 以几百倍速推演，输出里程碑、种群曲线，
并断言几条不变量（坐标不能�?NaN、幼虫不能跑出屏幕、食物真的会被啃光、成虫真的会被烂果子吸引）�?改完 `config.js` 想快速看效果就跑它。其中「存档往返」那一节会把世界序列化再恢复，
逐字段比对，并让恢复出来的世界继续跑 30 �?—�?只比数量的话，位置、年龄全丢了也照样「通过」�?
里面还有一�?*经济验证**：抽 20000 次看三档比例、走一遍体重曲线（出生 0.2mg �?满成长恰好等�?自己的上限）、确认体型不受体重影响、把 0.6× / 0.3× 两个倍率�?*飞行 / 爬行 / 罐中三条路径**�?分别量一遍、逐条比对售价公式和六个档位的边界值、`formatMoney()` 的几种写法，
以及「钱不够时买了不能既不扣款也不发货」这类结算规则�?
再往后是**投放经济**一节：三种单价和「单�?× 10」的浮点精度、撒 2000 次确认食�?*全部**
落在投放区内（只抽查两三个点是撞不出边界写错的）、超上限时按实际份数收费并退差价�?金苹果正好是 1.5× �?*普通苹果必须还�?1.0×**、以及「钱�?0 时一只果蝇也投不出来�?（破产保底已经取消，这条�?*反向**断言，防止它被好心加回来）�?
> �?金苹果那条断言�?*每帧重新钉住** `eating`：两�?update 都会把它清成 null 再按
> `world.nearestFood()` 重算，而测试喂进去�?Food 并不�?`world.foods` 里�?> 只在循环外设一次的话，只有第一帧算「在吃」，量出来是 **1.005** 而不�?1.5 —�?> 差别小到不容易一眼看出来，正是那种会静默混过去的假通过�?
```bash
bun run probe            # 单只果蝇 + 一份烂香蕉，逐秒打印它在干嘛
```

还有个调试开关：`bun start --autocontinue` 会跳过启动选择框、直接当玩家选了「继续」�?它的存在是因�?*读档 �?恢复 �?渲染 �?再存档这条链路没法进自检** —�?自检下存档功能是关掉的（免得用假世界覆盖真存档），所以那块一直是测试盲区�?改动存档相关代码之后，建议手动跑一次：先正常开局让它存一次档�?再用 `--autocontinue` 启动，确认世界接着上次继续、罐子和罐中果蝇都还在�?
`probe` 是排查飞行行为的专用工具：逐秒输出距离、坐标、朝向偏差、速度、有没有闻到味道�?上面那个「果蝇从不改变方向」的 bug 就是靠它定位�?—�?种群级的统计看不出这种问题，
但逐帧盯着单只果蝇一眼就发现了�?## 操作

工具栏是一�?318px 的小窗，主体是一排按钮，另外�?
| 操作 | 说明 |
| --- | --- |
| **拖标题栏** | 把这扇窗搬到任意位置（会自动夹在屏幕内，拖不丢） |
| **�?*（标题栏�?| 收起整个面板，只剩标题栏一条；再点一次展开 |
| **�?*（标题栏�?| 退出程�?|
| **拖动** | **戴上手套**（G）后按住屏幕上的食物 / 玻璃�?/ 烤串 / 烤炉 / 蛹壳就能拖走（指针会变成小手）。拖到窗口里的垃圾桶上松手即销毁�?*玻璃罐是例外，不用手�?*（见下） |
| **垃圾�?* | 物件悬在它上方时会变红，松手才真的丢�?|

主体里的按钮�?
| 操作 | 说明 |
| --- | --- |
| **工具 �?* | 工具组默�?*收起**（要占两行）。点这一行展开 / 收起。收起时右侧会显�?*当前拿着哪个工具**，快捷键也照常生效，不用展开 |
| **观察**（Esc�?| 只看不动。鼠标基本穿透，不挡你操作桌面�?*唯一例外**：指针停到玻璃罐上时窗口会接管鼠标，直接就能拖它（见下） |
| **查看**（V�?| 点一�?*成虫**，弹出它的数据面板（性别 / **价值档** + **体格** / 成长 / 体重 / 生命�?/ 售价 / 基因）。面板会**钉住**，直到你�?Esc、点空白处、换工具或者那只虫死掉�?*飞行中的成虫也点得开**，而且这一�?*不会惊飞果蝇** —�?否则手一动目标全跑了。⚠ **幼虫点不开**，见�?|
| **手套**（G�?| 拖东西的主力：食�?/ 烤串 / 烤炉 / 蛹壳 / 成虫都靠它。它没有「点一下」的动作�?*也不自绘光标** —�?直接用系统的手型指针（按下时变成抓着的手）。⚠ **玻璃罐不吃这一�?*，观察模式下直接拖就�?|
| **苍蝇�?*（F�?| 点一下，半径 28px 内的生命体全部拍死，爆出一片汁液并在原地留下污�?|
| **捕虫�?*（N�?| 点一下，把附近的成虫网进最近的玻璃罐�?*要先在商店花 $1.2 买下�?* —�?没买之前按钮是灰的虚线框，点了会提示去商�?|
| **烤制**（R�?| 买过打火机之后才出现的按钮，文字跟着档位变成 打火�?/ 喷火�?/ 烤炉。前两档是�?*碰到**地上的尸体就烤熟」（按住扫过去能一路烤），第三档变成「点一下摆一个烤炉�?|
| **图鉴**（右下角书本�?| �?*屏幕正中**弹出一张格子式的卡片，左边一栏是**全部食物**（真的是画出来的，不是色块），右边一栏是**全部基因**。基因格上写着它的效果和出现概�?—�?而且那些数字�?*从配置现�?*的，改了数值界面自动跟着变。关法同�?|
| **设置**（右下角齿轮�?| �?*正常模式**�?*烦人模式**之间切。烦人模式把生态上限拉�?50 倍（见下），立刻就生�?|
| **重置** | 会弹一张确认卡 —�?它会连存档一起清掉，所以不再是一次误触就能触发的�?|
| **抹布**（C�?| 按住�?*来回滑动**，擦掉尸体、污渍和蛹壳�?*按住不动是擦不掉�?* —�?计的是滑过的路程。腐烂越久越难擦 |
| **喷水�?*（W�?| 商店 $2 买下之后才出现�?*按住左键**喷出一道水线，碰到的尸�?/ 污渍 / 蛹壳**直接消失** —�?不用像抹布那样来回滑。水�?*以指针为中心向两�?*伸：**滚轮**改长度（100~400px），**Shift+滚轮**转方向（15° 一格）。⚠ 它只冲「抹布能擦的那三类」，食物 / 罐子 / �?/ 幼虫 / 成虫一律不�?|
| **三条�?*（统计行右侧�?| 展开 / 收起细分数量：成虫、幼虫、卵、食物�?*成虫总价�?*。收起时主面板上只留「存活」和「死亡」两个数 |
| **捐款小罐�?*（右下角�?| 面板右下角一个描线会流动的小罐子。点开�?*屏幕正中**弹出一张卡片，里面是支付宝收款码和「自愿捐款，理性消费哦」�?*完全自愿，和游戏里的任何东西都不挂钩**。关法：�?/ `Esc` / 再点一下小罐子 / 点面板别�?|
| **投放**（金色） | �?*屏幕正中**弹出投放卡，分两组：**食物�?*（苹�?$0.001 / 金苹�?$0.01）�?*其他**（果�?$0.005 / **玻璃�?*）。前三种每行能选「投 1 个」或「投 10 个」，按钮上直接写总价；买不起的会置灰。玻璃罐**免费**，点「摆一个」就摆，摆满 4 个时置灰。关法：�?/ `Esc` / 点面板别�?|
| **商店**（金色） | 同样�?*屏幕正中**弹出，分两组�?*工具�?*（捕虫网 $1.2 / 放大�?$1 / 喷水�?$2 / 打火机链 $3�?8�?16）�?*帮手�?*（警报器 $3 / 养蝇�?$2�?5）。分类和顺序全在 `CONFIG.market.shopCats` 里，加商品只要往那张表补一�?id |
| **养蝇�?*（商店） | 买了之后替你干两件重复劳动�?*Lv1�?2�?*：生态里一份食物都没有时自动投放；**Lv2（再�?$5�?*：再加自动出售。买�?Lv1 之后商店那一行会多一颗「配置」，点开是张居中小卡片，可以选投什�?/ 投几�?/ 卖哪一�?*价值档** / 含不含带突变�?|
| **出售�?*（金色） | 戴上手套，把**成虫**�?*烤好的果�?*拖到这块金色区域上松手就卖掉，换游戏币。垃圾桶**不收**成虫（拖过去是放走） |
| **数据面板** | �?*查看**工具（V）点一下虫就弹出来，钉在虫旁边。上面一行大字是**价值档**（普�?/ 罕见 / 稀�?/ 极稀�?/ 超级稀�?/ 传说生物，卡片配色也跟着它走，越贵越花哨），大字底下那行小字�?*体格**（轻�?/ 超重 / 巨兽）。两套档位不同来源，见下 |
| 置顶 | 让果蝇浮在所有窗口之上（`Ctrl+Shift+T`�?|
| 倍�?| 展开后是一条速度轴：**时停 / 1× / 2× / 5× / 10×**（`Space` 也是时停）。果蝇一个世代要 25 分钟�?0× 走完一代约两分半。不�?1× 常态时（时停或加速）标题行会变琥珀色提醒你 |
| 穿�?| 开启时只有工具栏接管鼠标；关掉则整块屏幕都归桌�?|
| 重置 | 清空重来 |

**全局快捷�?*（穿透状态下点不到按钮时的救生索，打包后也生效）�?
- `Ctrl+Shift+F` 切换鼠标穿�?- `Ctrl+Shift+T` 切换置顶
- `Ctrl+Shift+Q` 退�?
**开发快捷键**（只在窗口聚焦时生效）：

- `Ctrl+Shift+R` 重载渲染进程 —�?**�?`bun start` 下可�?*
- `Ctrl+Shift+I` 开 DevTools —�?打包后也保留

> 程序启动时会 `Menu.setApplicationMenu(null)` 干掉 Electron 的默认菜单�?> 无边框窗口虽然看不见菜单栏，但它的快捷键照样生效，而且全是桌宠的坑�?> `Ctrl+R` 会清空果蝇、`Ctrl+W` 直接退出、`F11` 把透明层切全屏�?> `Ctrl+±` 缩放页面会让画布坐标和果蝇位置全部对不上�?> 所以需要的快捷键改成白名单式显式注册�?
## 调参速查

**所有可调数值都�?`renderer/src/config.js` 这一个文件里**，按生态环节分了组�?改完存盘 �?在运行中的窗口按 `Ctrl+Shift+R` �?立刻生效�?（只有改 `main.js` / `preload.js` 才需要完全重启。）

### 最常改�?
| 想改什�?| 字段 | 当前�?|
| --- | --- | --- |
| 果蝇飞多�?| `adult.speedMin` / `speedMax` | 300 / 1700 px/s |
| 多久改一次飞行方�?| `adult.dartIntervalMin` / `Max` | 0.08 / 0.45 �?|
| 果蝇能活多久 | `adult.lifespanMin` / `Max` | 16 / 25 分钟 |
| 成虫多大 | `adult.sizeBirth` / `sizeMax` | 7 �?23 px |
| 爬行速度 | `walk.speed` | 58 px/s |
| 走走停停的节�?| `walk.boutMin/Max`、`pauseMin/Max`、`pauseChance` | 0.7�?.6 / 0.25�?.1 �?/ 0.45 |
| 多少时间在爬 | `behavior.landChance` / `landChanceNearFood` | 0.45 / 0.7 |
| 卵多久孵�?| `egg.hatchMin` / `hatchMax` | 3 / 14 分钟 |
| 卵的大小 / 胖瘦 | `egg.scaleMin/Max` / `aspectMin/Max` | 0.45�?.92 / 0.36�?.81 |
| **幼虫体长曲线** | `larva.sizeKeys` | 6/12/20 �?�?10/15/20px |
| 幼虫多久化蛹 / 羽化 | `larva.pupateAt` / `emergeAt` | 20 / 25 分钟 |
| 一次产几颗�?| `mating.eggsMin` / `eggsMax` | 2 / 6 |
| 时停 / 倍速档�?| `index.html` �?`#speeds` 里那排按�?| 时停 / 1× / 2× / 5× / 10× |
| **中间留多大空地不产卵** | `laying.centerClearRx` / `centerClearRy` | 0.345 / 0.357（屏幕比例） |
| 有多坚持要产在边�?| `laying.edgeWeight` / `edgeFull` | 2.6 / 1.7 |
| 有多常「就地开产�?| `laying.scatterChance` | 0.2 |
| 交配冷却 | `mating.cooldown` | 90 �?|
| 食物多久�?| `food.rotTime` | 9 分钟 |
| 一份食物多耐吃 | `food.larvaMealTime`（一只幼虫独吞要多久�?| 60 分钟 |
| 食物招蝇范围 | `food.flyScentRadius` | 600 px |
| **要不要自动投�?* | `food.autoSpawn` | `false`（关�?|
| 食物抓取手感松紧 | `food.grabPad` | 9 px |
| 尸体多难�?| `remains.wipeScrubFresh` / `wipeScrubRotten` | 70 / 300 px 的来回滑�?|
| 蛹壳多难�?| `pupa.shellScrub` | 420 px |
| 尸体多久烂�?| `remains.rotTime` | 6 分钟 |
| 屏幕最多多少只 | `world.maxAdults` / `maxLarvae` / `maxEggs` | 40 / 45 / 40 |
| 开局几只 | `world.initialFemales` / `initialMales` | 2 / 2 |
| 苍蝇拍范�?| `tools.swatRadius` | 28 px |
| 捕虫网范�?| `tools.netRadius` | 62 px |
| **投放的单�?* | `market.prices.spawnFly` / `.food.apple` / `.food.gold` | $0.005 / $0.001 / $0.01 |
| **金苹果的加成倍率** | `food.growthBonus.gold` | 1.5（普通苹果是 1，别动） |
| **食物投放�?* | `food.zone` �?`x` / `y` / `w` / `h` | 0.01 / 0.01 / 0.21 / 0.39（屏幕比例） |
| 让投放区显示出来 | `food.zone.show` | `false`，改 `true` 出调试虚线框 |
| **食物尺寸区间** | `food.sizeMin` / `sizeMax` | 24 / 200 px�?*绘制直径**，实际半径最�?`×0.61`�?|
| **「正常」有多大 / 占多�?* | `food.sizeNormalMax` / `sizeNormalChance` | 30 px / 95%（剩�?5% �?31~200px 的巨型） |
| **巨型里越大越少到什么程�?* | `food.sizeBias` | 3（巨型的中位�?115px；调大更极端�?*只管巨型那一�?*�?|
| **耐用性最多几�?* | `food.durabilityMax` | 20（最大的果子是普通果子的 20 倍营养） |
| 啃光后污渍的单块上限 | `food.stainMaxSize` | 34 px（不封的�?200px 果子会留 124px 的黑斑） |
| 一份食物同时供几只幼虫 | `food.eatersPerSize` | `size × 2` �?48~400 �?|
| 同时最多几份食�?| `food.maxCount` | 30�?00px 的果子多了会吃填充率，卡就先调这个） |
| 一次投放的档位 | `ui.js` �?`FEED_QUANTITIES` | `[1, 10]` |
| **喷水�?*长度 / 转向步长 / 命中放宽 | `tools.squirtLenMin` / `Max` / `Step` / `TurnStep` / `squirtRadius` | 100 / 400 / 20 px / 15° / 10 px |
| **商店和投放的分类** | `market.shopCats` / `feedCats` | 分组 + 渲染顺序的唯一来源 |
| **玻璃罐大�?/ 盖子** | `jar.width` / `height` / `capWidth` / `capHeight` | 194 / 246 / 134 / 32（整�?194×278�?|
| 罐中寿命倍率 | `jar.lifespanBonus` | 2 |
| 警报器价�?/ 两档阈�?| `market.shop` �?alarm �?`price` / `warnOrange` / `warnRed` | $3 / 0.2 / 0.1 |
| 警报器配�?| `visual.alarmOrange` / `alarmRed` | �?|
| 玻璃罐配�?| `visual.jarRim`（半透明天蓝�? `jarCap`（实心棕�?| 见「玻璃罐：盖子和尺寸是怎么来的�?|
| **成虫配色 / 材质** | `visual.bodyColor*` / `eyeColor*` / `wing*` / `legColor` | 见「成虫体表材质�?|
| **查看工具点虫的命中半�?* | `tools.hoverRadius`（进入）/ `inspectReleaseRadius`（离开�?| 16 / 28 px |
| 抓取成虫的命中半�?| `tools.grabFlyRadius` | 26 px |
| **四种突变的概�?/ 效果** | `mutation.types[]` | 2.9% / 3% / 2% / 1.1%，见「基因突变�?|
| 突变遗传�?| `mutation.inheritChance` | 0.5（孟德尔分离比） |
| 石化蝇拖拽的跟手程度 | `tools.stoneDragFollow` | 0.12（每帧朝指针靠拢的比例） |
| 疯狂的发作间�?/ 波及半径 | `mutation.types[]` �?berserk �?`intervalMin`/`Max` / `radius` | 16~36 �?/ 46 px |
| 疯狂蝇的寿命倍率 | `mutation.types[]` �?berserk �?`lifespanMul` | 0.5 |
| **养蝇人两级的价钱** | `market.keeperChain[]` �?`price` | $2 / $5（逐级累加�?|
| 养蝇人的可选项 | `market.keeperOptions` | 投什�?/ 投几�?/ 卖哪�?/ 含不含突�?/ 多久查一�?|
| 蛆有几成是「外虚内实�?| `larva.translucentChance` | 0.3 |
| 出生体重 | `market.birthWeight` | 0.2 mg |
| **售价公式** | `market.pricePerMg` | 0.01（每 0.1mg �?$0.001�?|
| 三档**体格**的出现几�?/ 满成长体�?/ 速度倍率 | `market.rarity[]` | 见上表（`name` 是叫法，`id` 别改�?|
| 六档卡片配色 | `market.valueTiers[]` | 见上�?|
| 放大镜价�?/ 高亮门槛 | `market.shop[]` �?`price` / `minValue` | $1 / $0.5 |
| 所有颜�?| `visual.*` | �?|

### 时间单位（最容易踩的坑）

`config.js` 里的时长**一律是毫秒**，文件顶部定义了 `SEC` �?`MIN`�?所以写 `2 * MIN` 才是 2 分钟�?*直接�?`2` 就是 2 毫秒**，果蝇会瞬间死光�?
### 三种改法

**�?改文�?* —�?推荐，会保存下来�?
```bash
bun start          # 起起来，�?config.js，然�?Ctrl+Shift+R
```

**�?运行时临时试** —�?不改文件，关掉就没了。按 `Ctrl+Shift+I` 开 DevTools�?
```js
__pet.world.timeScale = 8                                  // 8 倍速，几秒看完一个生命周�?__pet.world.dropFoods('gold', 3)                           // 立刻�?3 个金苹果（不收钱，这是内部投放）
__pet.world.money = 100                                    // 给自己发点钱，试投放面板
__pet.world.flies.forEach(f => f.lifespan = 20000)         // 让所有果�?20 秒后�?__pet.world.flies[0].mode = 'walk'                         // 强制某只落地爬行
__pet.config.food.zone.show = true; __pet.ui.refreshFoodZone()  // 把食物投放区画出�?__pet.config.food.growthBonus.gold = 5                     // 金苹果改�?5 倍速，立刻生效
```

> `__pet.world.flySpeed = ...` 这类是无效的 —�?速度不叫这个名字，也不挂�?world 上�?> 想改数值就�?`__pet.config` 里对应那一项（它是活的，改完下一帧就生效），
> 或�?�?改文�?+ 重载�?
**�?无头验证** —�?改完跑一遍，几秒钟就知道有没有把生态改崩：

```bash
bun run sim 120
```

它会输出里程碑和种群曲线，并断言一堆不变量（体长曲线对不对、招蝇有没有失效�?爬行有没有退化成一格一格、遗传会不会漂移……）。退出码�?0 就是有问题�?
### 一组必须联动的�?�?
**改飞行速度时，这三个值要一起看**，否则会有隐蔽的功能失效�?
| 联动�?| 位置 | 不联动会怎样 |
| --- | --- | --- |
| 配对扫描间隔�?0ms�?| 写死�?`world.js` �?`step()` �?| 果蝇一帧飞太远，擦肩而过的配对会被漏�?|
| `food.flyScentRadius`�?00px�?| `config.js` | 半径太小 �?果蝇 0.3 秒就穿出去了，闻不到味道 |
| `behavior.forageWalkMin/Max`�?�?2 秒） | `config.js` | 落地后走不到食物跟前就又起飞，看着像没头苍�?|

**这三处已经在三次提速里各踩过一�?*，所以单独列出来�?
## 生态机�?
| 阶段 | 行为 |
| --- | --- |
| **�?* | 母体原地产下 2�? 枚，3�?4 分钟后孵化，落地后不再移动。大小、胖瘦�?*深浅**都逐颗随机�?*纯色填充、无描边** |
| **幼虫（蛆�?* | 三个龄期�?**6 / 12 / 20 分钟**结束，体�?5 �?10 �?15 �?20px。在屏幕内匀速爬行，身体�?*一条平滑的曲线、不分节**，整条是**纯色**（没有描边、没有渐变），头尾都�?*圆头** |
| **蛆的软身�?* | 幼虫�?(x, y) 就是**�?*的位置，身体是被�?*拽着�?*的：一串存�?*世界坐标**里的点（脊柱），每个点只被约束在离前一个点恰好一个体节处、自己不主动移动。所以转弯时身体会弯（实测滞�?84%），直行时是直的 |
| **幼虫体型** | 同一批（同一母体产的）长得像，不同批次差别一眼能看出来：有的�?*短胖椭圆**（宽长比�?0.39），有的�?*细长米粒**（约 0.16），相差 2.4 倍。体长另�?±18% 的个体差异；蛹也沿用同一性状 |
| **集群** | 每隔 25�?0 秒触发一次，75% 的幼虫会聚集到同一个点，持�?15�?0 秒�?*和「组队」是两回�?*：集群是世界级的偶发事件，组队是同窝幼虫的日常结�?|
| **食物** | 一�?*形状不规�?*的苹果屑。每块的轮廓由自己的 seed 生成�?�?0 个顶点，角度半径都不同），所以同屏几块形状各不相同�?*不再自动投食** —�?只有你点「投放」才会出现（同时最�?30 份） |
| **食物投放�?* | 所有食�?*只落在屏幕左上角一块矩形里**（横 1%~22%、纵 1%~40%，见下）。开局那份、自动投放、玩家买的，一视同�?|
| **金苹�?* | 加了它才有的第二种食物。样子和普通苹果一样、只是金�?+ 一圈很淡的光晕�?*在它上面进食时年龄跑得快 1.5 �?* —�?幼虫更快化蛹羽化，成虫更快长大变重、卖得更贵，�?*寿命也同比缩�?*。它不是自动投放的，只能�?$0.01 �?|
| **幼虫的嗅�?* | **间歇采样**，不是每帧盯着食物看：每只按自己的节奏�?0.4~1.6 秒「闻」一次，两次之间�?*记住的位�?*爬；每只还有自己的灵敏度（半�?× 0.65~1.35）。所以拖动食物时，幼虫不会齐刷刷同时掉头，而是各自把老地方爬完、各自重新闻到、各自掉头（见下�?|
| **觅食** | 幼虫�?170px 内闻到食物就爬过去（每只还要乘自己的灵敏�?×0.65~1.35），爬到果实**外围**的落点后边走边拱。每只幼虫记自己的方位角，所以是分散趴在果子上，不会全挤到圆�?|
| **进食** | 一只幼虫独吞一份食物要 **60 分钟**，同时有几只在啃就快几倍�?*能同时进食的只数按果子体积算**（`size × 2`，见「关于抢食与饿死」），挤不进去的会散开去别处游荡，不会赖在果子�?|
| **招蝇** | 果子腐烂 15% 后开始散味�?*闻到味道的瞬�?*就掷骰子决定要不要落下去�?00px 内），落地后这一趟会走得更久，所以能一路走到食物跟�?|
| **成虫进食** | 走到果子上的成虫**不会钉在圆心**，而是在食物周�?*随机踱步**：随机挑一个落脚点走过去，到了（或 0.4�?.6 秒后）再挑下一个，速度降到 60%。活动半�?= `food.feedRadius` × 食物尺寸（约 30�?8px，是果子轮廓半径�?3 倍），所以它�?*拱到果子上、再踱到旁边、又拱回�?*。随机点�?`√rand` 铺开，几只果蝇是各自散开，不会叠在正�?|
| **幼虫在哪�?* | 落点在果子的**外围**，不在圆心（`food.restRing` = size×0.45）。而且**边走边啃** —�?�?1.5~4 秒换个位置，方位角和半径都重抽。早先是「走到自己那个落点就永远停住」，几只坐在同一个半径、各自一个固定方位角、一动不动，远看就是一排钟点数�?|
| **吃剩** | 被啃光后原地留下 3 块污渍，需要用抹布收拾 |
| **蛹期** | 20�?5 分钟完全静止。形状是一�?*扁长的米**（宽长比�?0.24，比幼虫还细），�?*淡淡一层轮�?*和壳面颗�?|
| **化蛹变色** | 刚化蛹时还是幼虫的奶白色�?*�?30 秒内慢慢变成褐色** —�?「看着一条蛆变成一粒褐色的蛹」本身就是有信息量的画面，所以这一段单独给时间，不摊到整段蛹期里。之后到羽化前再往深褐走一点点 |
| **蛹壳** | 羽化之后成虫飞走，原地留下那�?*空壳**：形状是**瘪的**（随机塌陷，见下）、颜色比活蛹浅、轮廓上留一个缺�?= 钻出来的洞。清理有**两条�?*：抹布来回擦（比烂尸体更费劲，它是一枚硬壳），或者戴手套拖进垃圾�?|
| **空壳的塌�?* | 每一枚壳随机凹进�?1~2 处（`pupa.shellCollapse`），位置 / 深浅 / 宽窄逐枚不同�?*这是分辨空壳最快的一条线�?* —�?颜色得比一比才看出来，形状扫一眼就知道 |
| **成虫** | 25 分钟羽化，体长从 7px 一路长�?23px，寿�?**16�?5 分钟** |
| **飞行** | 300�?700 px/s，随机的「窜一下、停一下」，飞出屏幕后从**中心对称**的位置飞回来 |
| **步行** | 果蝇�?35% 的时间落在地上爬�?8 px/s（只有飞行的 1/30），**位移连续**，但按「走一�?�?停一下重新定�?�?再走」的节奏（约 12% 的帧是停顿） |
| **�?/ 走切�?* | �?1.2�? 秒后有一次落地机会（平时 45%，附近有腐烂食物�?70%）；**闻到气味的瞬�?*额外掷一次骰子，所以飞过食物上空时大概率会落下 |
| **交配** | 雌雄靠近即算交配�?*没有任何交配动画**，双方进�?90 秒冷�?|
| **产卵** | 母体**先飞到屏幕边缘的产卵�?*（见下），再减速停住（翅膀张开、只慢慢扇），每 0.7 秒落一颗卵�?�? 颗产完再飞走�?*卵尽量不落在屏幕中间** —�?桌宠不能挡着玩家干活 |
| **死亡** | 不管老死还是**被拍死，都会留下尸体**（被拍死的不再凭空消失），另外还会溅出汁液和一小块汁渍。尸体和污渍一样要用抹布擦，且越烂越难�?|
| **腐烂** | 尸体 6 分钟、污�?4 分钟达到完全腐烂，擦掉它需要的来回滑动�?70px 涨到 300px |
| **存档** | �?15 秒自动存一次，关窗�?/ 退出前再补一次。下次打开时如果读到存档，先问「继续上�?/ 重新开始」再开始跑 |
| **玻璃�?* | **投放**弹窗 →「其他」→ 点「摆一个」（免费，最�?4 个，摆满时按钮置灰）。可以用鼠标拖着摆位置、拖进垃圾桶扔掉。⚠ 它原来在工具栏上是一颗独立按钮，现在撤掉�?—�?同一个功能留两个入口的话玩家得记两个地方。罐中的果蝇**完全隔离**：不交配、不产卵、苍蝇拍也拍不到�?*罐中寿命是外面的 2 �?* |
| **怎么把果蝇装进罐�?* | 两条路：**捕虫�?*（N）点一下，半径 62px 内的成虫进最近的罐子（一网打尽）；或�?*戴手套把某一只拖到罐子上松手**（精确操�?—�?存稀有品种靠这条）。拎着蝇压到罐子上时，那个罐子会变成金色告诉你「松手就进�?|
| **警报�?*（商�?$3�?| 买了之后�?*罐中**果蝇寿命�?20% 亮橙光、剩 10% 亮红光（会呼吸，越快到头闪得越急）。外面的成虫不提�?|
| **罐子形状** | **平面 2D 矩形**，罐�?194×246、盖子在它正上方 134×32（整�?194×278，约占屏幕宽 10%、高 25.7%）。罐身是一�?*半透明天蓝**的圆角轮廓，盖子是一�?*实心棕色**。刻意不模仿 3D 圆罐 —�?整屏其余部分都是扁平的（连生命体都刻意不带投影），罐子单独做成立体的反而格格不�?|
| **罐中飞行** | 罐里的果�?*是飞着�?*（翅膀张开、扑腾），速度 60~150px/s —�?只有外面巡航�?00~1700）的十分之一，再快就糊成一道影子、而且一直在撞玻璃。撞到四条边会反�?|
| **窝（clutch�?* | 一次产�?= 一窝�?*母体先挑一个地方飞过去再产**�?*主要看「靠屏幕边缘�?*，其次离尸体污渍远、别压在别人的卵堆上），偶尔也会偷懒就地开产。一窝共用一个窝号，这个号一路跟到幼虫身上：**同窝的卵差不多同时孵**�?*同窝的幼虫更容易凑到一起走**（也不排除跟别的幼虫�?|
| **捕虫�?* | 拿网点一下，半径 62px 内的成虫会被装进**最近的、还有空位的**罐子。一个罐子最多装 8 只。没有罐子或罐子满了会在提示条上说明原因 |
| **罐中寿命** | 罐中果蝇的寿命是外面�?**2 �?*。实现方式是**时间膨胀**（年龄推进速度变成 1/2），所以进出罐子完全可逆，反复进出也不会漂移。⚠ 罐中列表里的「已�?/ 寿命」是按这个倍率换算回真实时间的，改 `jar.lifespanBonus` 时那边会自动跟上 |
| **罐中列表** | 场上只要有罐子，**右上�?*就出现一扇独立的「罐中果蝇」小窗（没有罐子时整扇窗都不出现）�?*默认收起成一条标题条**，点一下才展开。每行显示罐号、性别、成长进度条 + 百分比、剩余寿命、售价，右侧是「出售」和「放逐」。拖标题条可以挪位置 |
| **一键出�?/ 一键放�?* | 列表上方两颗按钮，一次处�?*所有罐子里的全部果�?*。养大一批一起卖是最常见的用法，一行一行点太磨人。⚠ **「全部出售」会先弹一张确认卡** —�?卖是不可撤销的，而罐子恰恰是你特意存下来的地方，风险和「重置」是同一类；「全部放逐�?*不确�?*，因为虫子只是回到屏幕上，随时能再网回去 |
| **体格（体重档�?* | 每只成虫**羽化那一�?*抽一次：**轻盈 95%**（满成长 0.8~1.4mg）�?*超重 4.5%**�?0~50mg）�?*巨兽 0.5%**�?~10g）。出生体重一�?**0.2mg**，随成长进度线性长到自己那条上限（见下�?|
| **体重 �?体型** | 体重**完全不影响画多大**。一�?10g 的巨兽和轻盈蝇一样是 23px —�?体型只由成长进度决定 |
| **体重影响速度** | 超重 **0.6×**、巨�?**0.3×**（轻�?1×）。飞�?/ 爬行 / 罐中三种移动**全部**乘这个倍率，所以一只巨兽飞起来是慢悠悠�?90~510px/s |
| **售价** | **体重�?0.1mg = $0.001**（等价于 `mg × 0.01`）。一只满成长的轻盈蝇�?$0.008~$0.014，一只巨�?$10 ~ $100 |
| **出售** | 戴上手套�?*成虫**拖到金色出售区松手。换到的钱按上面那条公式算，果蝇当场消失并计入「已售」。垃圾桶**不收**成虫 |
| **商店** | 面板里点金色「商店」按钮展开�?*放大�?$1** 买下之后，全场价�?�?**$0.5** 的成虫身上会亮一�?*白色**脉冲光环 —�?**屏幕上的、罐子里的、烤炉里的都�?* |
| **数据面板** | �?*查看**工具（V）点一下虫弹出（价值档 + 体格 / 成长进度�?/ 体重 / 售价）。卡片按售价分六档配色，越贵越花哨（见下�?|

### 关于体格（体重档�?
**为什么体重不碰体型�?* 体型已经有一套很密的信息了（成长进度、每只的遗传宽长比�?幼虫期还有三个龄期），再把体重也画进去，两只 23px 的蝇就得靠眼力比大小才分得出谁贵�?现在的分工是�?*体型 = 你养了多久，速度 = 它值多少钱**�?超重的蝇一眼看得出�?—�?它飞得比别人慢半拍，这是屏幕上最直接的线索�?
�?体格�?*羽化那一�?*才抽的，不是产卵的时候。只�?`Fly` 才有 `rarity`
（`Egg` / `Larva` 上根本没有这个字段），所以数据面板上那条蛆既没有体格也没有价值档 —�?它现在还不知道自己将来多沉。这是有意的：一窝里蹦出什么，得养到羽化才开奖�?
**上限是每只自己抽的，不是档位共用一个�?* `weightMax` 在出生时抽一次存进实体，
之后一辈子不变。所以同档位的两只轻盈蝇，一只满成长 0.83mg、一�?1.37mg�?它们的售价差 65%。这个「抽一次、存下来」的做法还有个好处：**存档读档不会重抽**�?今天看它�?$0.013，明天打开还是 $0.013�?
**体重曲线用的是现成的成长进度�?* `weightAt()` 就是 `lerp(0.2, weightMax, growth)`�?`growth` �?`age / lifespan` —�?和体型用的是同一个值，没有第二套计时�?代价�?*寿命长短直接影响能长多重**：一只寿�?2 分钟的蝇和一只寿�?18 分钟的蝇�?满成长都是自己的 `weightMax`，但前�?2 分钟就到顶、后者要 18 分钟�?这是故意�?—�?一只活得久的蝇本来就更值钱，而且它也更难（你得等它长大，还得在这期间别把它拍死）�?
**「巨兽」是真的稀有�?* 0.5% × 40 只上�?�?场上平均 0.2 只�?一局里能不能见到一只，看运气�?
**放大镜的门槛为什么从 $10.1 降到 $0.5�?* 原来�?10.1 是「金色档及以上」，
也就�?*只有巨兽会亮** —�?而那只有 0.5%。结果是买完放大镜之后，
绝大多数局里它一次都不会亮，玩家只会以为这件道具坏了�?$0.5 这一档是照着**实际能养出什�?*选的：超重蝇满成长是 $0.1 ~ $0.5�?所以门槛压�?$0.5 之后「养得够大的超重蝇」开始亮，而轻盈蝇�?0.002 ~ $0.014）仍然不�?—�?高亮还是「这只值得看一眼」的意思。要再放宽就�?`CONFIG.market.shop` �?magnifier �?`minValue`�?
**光环是白色的，而且罐子 / 烤炉里的也一起亮�?* 光环原来按价值档上色�?但分档色本身带着「越贵越花」的含义，套在一个会呼吸的光环上读起来像**又一层稀有度**�?白色是这里唯一没有别的东西在用的高亮色，一眼就知道是「工具标的」�?另外容器里的果蝇算的也是「全场」—�?罐子本来就是你特意存下来的那批，不给它高亮才奇怪�?�?实现上有个坑：罐中和炉中果蝇�?`x` / `y` �?*相对容器中心**的偏移，
要加上容器自己的坐标才是屏幕坐标，漏了的话光环会全叠在屏幕左上角（见「放大镜」注释）�?
### 关于食物尺寸与耐用�?
**尺寸是两段，不是一段连续分�?*（`entities.rollFoodSize()`）：

| �?| 区间 | 占比 | 段内怎么�?|
| --- | --- | --- | --- |
| 正常 | `24 ~ 30px`（`sizeMin ~ sizeNormalMax`�?| **95%**（`sizeNormalChance`�?| 均匀 |
| 巨型 | `31 ~ 200px`（`sizeNormalMax+1 ~ sizeMax`�?| 5% | **越大越少**，`random^sizeBias`（`sizeBias = 3`�?|

两段**既不留空隙也不重�?*：正常段抽到 `[24, 30]`，巨型段�?31 起，
所以场上永远不会出�?30~31 之间的尺寸（`sim` 有一条专门查这个）�?
> �?原来是一整段 `24~200px` 配一个偏向小端的指数。那套算下来「正常大小」的果子
> 只占七成�?—�?**满屏都是明显偏大的果�?*，「偶尔见到一个巨无霸」变成了
> 「一半的果子都不正常」。拆成两段之后，「绝大多数就是普通果子」和
> 「巨型很罕见」这两件事可以各自调（`sizeNormalChance` �?`sizeBias`），
> 不再互相牵制�?
`sim` 里四个统计量分别守四件事：段占比、两段区间（含「没有空洞」）�?巨型段内的偏斜（比的�?`u³` 的均�?—�?均匀时是 0.5，三次方后是 0.25）�?以及巨型确实够罕见。改回均匀分布会立刻红，而且报出来的数是 0.5 而不�?0.25�?
**耐用性最�?20 倍，而且「啃完的时间」正好也�?1~20 倍�?*
一份食物的营养总量�?`1 ~ durabilityMax(20)` 之间按尺�?*线�?*插值，而啃完时间是

```
啃完时间 = larvaMealTime × durability ÷ 同时进食的幼虫数
```

`larvaMealTime` 是「一只幼虫独吞一整个�?60 分钟」。所以对固定的一批幼虫，
时间倍率就是 `durability` 的倍率�?
| 尺寸 | 耐用 | 45 只一起啃 |
| --- | --- | --- |
| 24px | 1× | �?1.3 分钟 |
| 30px（正常段上限�?| 1.7× | �?2.2 分钟 |
| 200px | 20× | �?27 分钟 |

�?**20 倍是�?5% 的巨无霸才够得着的�?* 因为 durability 是按 `[sizeMin, sizeMax]`
整段线性插值的，�?95% 的果子落�?`24~30px`，所以绝大多数果子的耐用度其实在
**1 ~ 1.7 �?*之间。这是两段分布的直接后果，不�?bug —�?想让普通果子也经啃�?就调 `sizeNormalMax` 那一档（比如放到 60），代价是「正常」看起来不再正常�?
�?**同时能站的幼虫数没有�?*（还�?`size × eatersPerSize`）。所�?200px 的果�?能同时容�?400 只，只是场上一共也没那么多幼虫 —�?于是「大果子」的手感就是
**一顿管很久**，而不是「一顿喂完全场」�?
> 这一节最早是按「大苹果会变成几十小时啃不动的永久地标」来设计的，
> 后来发现那个数是**算错�?60 �?*（把分钟当成了小时）�?> 实际 27 分钟完全合理，所以给「同吃数封顶」那套补丁根本没�?—�?不需要�?
�?**`Food.nutrition` 的语义是「剩余总量」，不是「剩余比例」�?*
想要「啃掉了几成」请�?`Food.eaten` getter。渲染那边就是这个坑�?`drawFood` 早先写的�?`1 - f.nutrition`，`nutrition = 20` 时会得到 `-19`�?果子被画�?**5 倍大**。小果子上两者恰好相等，所以这个错**只在 200px 的果子上�?*�?
**连带改掉的几�?*（放开�?200px 会撞上的老假设）�?
- **投放区的内缩�?*�?*绘制半径**算（`size × FOOD_DRAW_RADIUS = 0.61`），
  不是 `size/2`。苹果的轮廓顶点伸到 `size/2 × 1.22`，按 `size/2` 内缩的话
  200px 的果子会压出投放区、甚至切到屏幕外。这个常量在 `config.js` 里，
  `render.js` 的顶点系数也从它反推 —�?两处各写一份迟早会�?- **啃光后的污渍**尺寸封顶�?`stainMaxSize`。不封的�?200px 的果子会留下
  124px 的黑斑（比工具栏面板还大），三块叠一起能糊掉小半个屏�?  �?散布半径**不封�?*：大果子啃光本来就该脏得更开

�?**投放区只有屏幕的 21% × 39%**�?920×1080 下是 403×421）。一�?200px 的苹�?几乎顶格 —�?想吃到大果子，就得接受它占地方。这是「别挡住屏幕中间干活�?那条产品决定的必然代价，不是没调好�?
### 关于喷水�?
它是**抹布的升级版**：抹布要按住来回滑（计的是路程，越烂越难擦）�?喷水枪一碰就没。定位很清楚 —�?后期满屏尸体时，一块一块擦太磨人�?
三个设计点：

1. **水线以指针为中心向两头伸**（不是从指针射出去）。想冲哪里就把指针放哪里�?   不用先转方向对上；旋转只用来改扫过的角度�?   �?由此有个几何事实�?*不管转到什么角度，水线永远穿过指针那一�?* —�?   所以「转方向」对指针正下方的目标没有任何影响。自检里那条「转 90° 之后
   就不该再命中」的断言，目标必须放�?*偏离指针**的位置，否则测不出区�?2. **判定�?world �?*（`world.squirt(x1,y1,x2,y2)`），理由�?`wipe` / `swat` 一样：
   sim 能直接断言，不用起 Electron。它算的�?*点到线段**的距�?   （把点投影到线段上，`t` 夹在 `[0,1]`），所以线段外会正确地退化成到端点的距离
3. �?**只碰 `remains` �?`shells` 两个数组**，置 `dead` 就够 —�?   不要�?`wipe()`（那条路要累计路程），也**不要顺手�?`wipeTrail`**
   （那是抹布的拖尾，纯表现，不是可清洁物）。食�?/ 罐子 / �?/ 幼虫 / 成虫一律不�?
�?**滚轮只在窗口接管鼠标时才转发进来**，而「手里拿着工具」正好会让窗口接�?—�?所以不必额外判定。但这也意味着�?*拿着别的工具时滚轮改不了水线**，这是预期的�?另外这个监听必须写成 `{ passive: false }`，否�?`preventDefault()` 是静默无效的�?**Shift 的按下状态不用自己维�?*：`WheelEvent` 自带 `e.shiftKey`�?
### 关于「投�?/ 商店」弹窗和分类

两块面板原来挤在工具栏小窗里当折叠区，展开就把垃圾桶和提示条顶到看不见�?现在搬到**屏幕正中**，和设置卡共�?`.donate-pop` / `.donate-card` 那一套�?
�?**它们必须挂在 `#hud` 下、不能待�?`.window` �?* —�?那扇窗有 `overflow: hidden`
（用来裁圆角），放进去会被整个剪掉。而卡片、列表、按钮在 DOM �?*全都�?*�?所有别的断言照样绿，只有肉眼看得见。自检里专门有一�?`closest('.window')` 守着它�?
**分类是数据驱动的**，`CONFIG.market.shopCats` / `feedCats` �?*唯一来源**
（同时决定分组和渲染顺序）：

```js
shopCats: [
    { id: 'tool', name: '工具�?, items: ['net', 'magnifier', 'squirt', 'roast'] },
    { id: 'help', name: '帮手�?, items: ['alarm', 'keeper'] },
],
```

�?`items` 里的 id 跨两种东西：`market.shop` 里的一次性道具，�?*可升级链**的链�?（`roast` / `keeper`）。加一件商�?= 往某一行补一�?id�?**漏了** �?商品从商店里凭空消失�?*重了** �?渲染两遍�?两种都不报错，所�?sim 里有一条断言盯着「每件商品恰好出现在一个分类里」�?
�?顺带一个必踩的坑：投放 / 商店 / 图鉴�?*入口按钮都在 `#panel` �?*�?点它会先打开弹窗、再冒泡�?`#panel`，�?`#panel` 的「点别处就关掉」逻辑判的�?「在不在入口按钮上」。不把它们加进排除列表，弹窗会在**同一次点击里**开了又�?—�?症状是「点了没反应」，而卡片其实闪开了一下。这个坑在设�?捐款按钮上已经发生过一次�?
另外加了�?`_closeCenterCards(except)`：每张居中小卡打开时先关掉别的�?**最容易踩的一对是「商�?�?养蝇人配置�?* —�?配置按钮就在商店列表里，
点下去不关商店的话，两张卡会叠在屏幕正中（它们全都是 `left:50% top:50%`），
�?`#keeper-pop` �?DOM 里排�?`#shop-pop` **前面**，同 `z-index` 下后出现�?画在上面 —�?商店会把养蝇人卡整张盖住，玩家看到的是「点配置没反应」�?这一对由 `setKeeperOpen()` 调用 `_closeCenterCards()` 处理�?
### �?居中小卡的「点击接管」是按矩形算�?—�?别让卡片比盒子宽

`ui._overCard()` 决定「指针压在卡片上时这扇窗口要不要接管鼠标」，
�?*不接管就收不到那一下点�?*（窗口是 `setIgnoreMouseEvents` 穿透的�?穿透时只有 `mousemove` 会被转发进来）。所以卡片可见区域和接管矩形
**必须重合**，差一点点就是「看得见、点不着」�?
踩过一次，症状�?*三张新卡�?�?一颗都点不动、弹窗关不掉**，而且没有任何报错�?
- 宽度原来写在卡片上（`.shop-card { width: 336px }`），而外�?`.donate-pop`
  �?CSS 里写�?268px。卡片是块级子元素，**从容器左边起�?*，不是居�?—�?  于是整张卡往右探�?68px，屏幕上的弹窗其实偏�?34px
- `_overCard()` 当时量的�?`.donate-pop` 那个 268px 的盒子，探出去的那一�?  不算「在卡片上」→ 不接管鼠�?�?点击穿到桌面
- �?恰好贴在卡片右上角，**整颗都落在探出区�?*，于是全军覆�?
两条规矩定下来：

1. **宽度只有一个来�?*：写在外�?`.donate-pop` 的修饰类�?   （`.wide` 336px / `.mid` 292px），卡片自己不写宽度、撑满它
2. `_overCard()` 量的�?*卡片**（`pop.querySelector('.donate-card')`），不是外层盒子 —�?   两者理论上相等，但那是约定不是保证，认元素才认的是「玩家眼里的卡片�?
自检里对应三条断言�?*都走命中判定而不�?`element.click()`** —�?后者会**绕过**命中判定，按钮被盖住也好、屏幕没被接管也好，它照样触发监听，
所以当年那些断言全绿�?bug 就在眼前�?
- 卡片中心必须落在屏幕正中（偏了就是上面那个「整张往右探」）
- `document.elementFromPoint(�?的中�?` 打出来必须还�?�?自己
- 指针压在 �?上时 `_overCard()` 必须�?true

### 关于图鉴

格子式列�?*全部**食物和基因，不做「解�?/ 未发现」那一套�?
�?这是整个项目�?*第一�?`display: grid`**。别处的换行排布一律用 `flex-wrap`
（那些都是「一排小按钮，排不下就换行」），而图鉴要的是**严格对齐的两�?*�?找不到现成的格子样式可抄是正常的，不是漏看了�?
�?**食物格是真的画出来的**（每格一个小 canvas），和场上那一份共�?`render.drawFoodIcon` —�?图鉴存在的意义就是「让我认得出屏幕上那个是什么」，
画得不一样就白做了。自检里那条断言**读的是像�?*（统计非透明像素），
因为「canvas 建出来了但一个像素都没画」用元素查询是查不出来的�?
�?**文案�?config 现算**，不另抄一份：倍数、概率、价格全是现场拼的�?抄一份的话改了数值图鉴就在说假话，而图鉴恰恰是玩家用来「查这个世界有什么」的地方�?
### 关于经济（出�?/ 商店 / 数据面板�?
**游戏币的显示格式�?`$xxx,xxx.xxx`�?* 千分位逗号�?*至少**三位小数、上不封�?（`formatMoney()` �?`renderer/src/market.js`）。整数部分没有上限，
所以以后要是加了别的收入来源，格式不会先崩�?
**六档配色是数据驱动的**（`CONFIG.market.valueTiers`），不是散在 CSS 里的 if-else�?
| �?| 价值区�?| 颜色 | 效果 |
| --- | --- | --- | --- |
| 普�?| �?$0.02 | �?| �?|
| 罕见 | $0.02 ~ $0.1 | �?| �?|
| 稀�?| $0.1 ~ $10 | �?| �?|
| 极稀�?| $10 ~ $100 | �?| **边框流动** |
| 超级稀�?| $100 ~ $1000 | �?| 流动 + **反光扫过** |
| 传说生物 | > $1000 | 淡彩 | 流动 + 反光扫过 |

**和「实际能养出什么」的对照**（左列是体格，见下一节）�?
| 来源 | 满成长售�?| 落在 |
| --- | --- | --- |
| 轻盈�?· 无突�?| $0.008 ~ $0.014 | 普�?|
| 轻盈�?· 带突�?| $0.03 ~ $0.06 | 罕见 |
| 超重�?· 无突�?| $0.1 ~ $0.5 | 稀�?|
| 超重�?· 带突�?| $0.39 ~ $1.95 | 稀�?|
| 巨兽 · 无突�?| $10 ~ $100 | 极稀�?/ 超级稀�?|
| 巨兽 · 带突�?| $39 ~ $429 | 超级稀�?|

「带突变�? 价值类突变叠满 ×3.9（石�?×1.5 体重、结�?×2、金 ×1.3），
再乘附近金光光环�?×1.1�?
> ### �?「传说生物」现在一只都养不出来
>
> **全局天花板是 $429**（最重的巨兽 ×1.5 石化 ×2 结晶 ×1.3 �?×1.1 光环），
> 而传说生物的门槛�?**> $1000** —�?连一半都够不到�?>
> 这不�?bug，是两个数一起决定的必然结果�?*想让它够得着，改一个就�?*�?>
> | 改哪�?| �?�?�?| 天花板变�?|
> | --- | --- | --- |
> | `market.pricePerMg` | 0.01 �?0.03 | $1,287 |
> | `rarity.extreme.weightMax` | 10000 �?25000 | $1,073 |
>
> 调完 `bun run sim`，那行提示会自己消失�?*在那之前，这一档的配色和特�?> 是写好了但没人看得见�?* —�?sim 里那条断言会把这个事实**打印出来**
> （而不是静默通过，也不是报错），所以它不会变成一个没人记得的坑�?>
> `bun run sim` 还会把「本该够得着却一次都抽不到」单独判�?*故障**�?> 那种才是分界划歪了（最早的六档版就是这�?—�?�?/ 淡彩的上界是
> $1000 / $10000，而当时最贵的巨兽正好 $100，那两档从来没亮过）�?
**到这里「稀有度」和「价值档」是两个不同的东西了，别再混�?*

| | 是什�?| 取值范�?| 用在�?|
| --- | --- | --- | --- |
| `market.rarity`�?*体格 / 体重�?*�?| 出生时抽�?*体格档位** | **轻盈** 95% / **超重** 4.5% / **巨兽** 0.5% | 内部：决定体重区间和速度倍率。数据面板上那行**小字** |
| `market.valueTiers`�?*价值档**�?| 按当前售价切的分�?| 普�?/ 罕见 / 稀�?/ 极稀�?/ 超级稀�?/ 传说生物 | 数据面板上那�?*大字**、卡片配色、养蝇人的「卖哪档」筛�?|

**两者的档数都不一样（3 vs 6），而且没有任何一一对应关系** —�?「金」既可能是超重蝇的高段、也可能是巨兽的低段�?所以「颜�?= 稀有度」在这个游戏里不成立，别照着卡片的颜色去猜体格�?
**为什么要用两套词�?* 这三档原来叫「普�?/ 变异 / 极端变异」，有两个毛病：
「普通」和价值档的第一�?*撞名**（面板上并排两行都写「普通」，谁知道哪个是哪个），
而「变异」又�?*基因突变**（疯�?/ 点石成金 / 石化 / 结晶）听起来是一回事�?现在两边各用各的词，看一眼就知道哪行在说什么：

- **体格**（轻�?/ 超重 / 巨兽）→ 这只�?*长得多沉**，影响体重和速度
- **价值档**（普�?/ 罕见 / …）�?这只�?*值多少钱**

�?改的只是**显示�?*，id（`normal` / `mutant` / `extreme`）一个字没动 —�?存档里存的是 id 不是名字，所以改名不影响任何存档�?
养蝇人的筛选走的是 `valueTierOf(f.value)`，和数据面板显示的是同一个来源，
不会出现「面板写着稀有、按稀有筛却筛不出来」�?
�?**顺带一个真实的耦合**：「卖哪档」和「含不含突变�?*不是正交�?*�?一只蝇带上结晶（价�?×2）之后，它自己的价值档也会跟着抬一�?—�?所以设了「只卖稀有档」的人会发现带了结晶的那批怎么都不卖�?这不�?bug，是价值档筛选的固有性质；`sim` 里有一条断言专门演示这件事�?
**渐变边框是拿 mask 裁出来的，不�?`border-image`�?* `border-image` 不支持圆角，
而这�?UI 里所有方块都是圆角的。做法是：`::before` 铺一层渐变，再用
`padding` + `-webkit-mask-composite: xor` 把中间挖空，只留一圈边�?「流动」就是给那层渐变�?`background-position` �?`@keyframes`。红色和淡彩多一�?`::after`
做斜向的扫光（`sheen`）�?
**卡片不进鼠标穿透的判定�?* `_updateInteractive()` 决定「这扇窗要不要接管鼠标」，
`#inspect` **刻意不参�?* —�?悬停是高频事件，一旦卡片参与进来，桌面上划过的每一次鼠�?都可能被桌宠吞掉。卡片自己是 `pointer-events: none`，纯显示�?
�?**拖成虫时 `_dragTo()` 必须每帧跑，不能只在 mousemove 里跑�?*
食物 / 罐子 / 蛹壳被拖的时候自己不动，所以钉�?mousemove 上够了；**果蝇每帧都在自己�?*�?只在 mousemove 时钉的话，它会直接从指针底下飞走。所�?`ui.update()` 里每帧都调一�?`_dragTo()`（顺带把被拖的那只的 `vx/vy` 清零）�?
**售价为什么是「每 0.1mg �?$0.001」�?* 直接�?`mg × 0.01` 等价，但用户的原话是前�?—�?把它留在注释里，调参的时候不用反过来心算�?
### 关于投放与金苹果

**先说它解决的问题：钱原来只进不出�?* 商店只有一�?$1 的放大镜，买完之后游戏币就再没有
用处，攒钱也就没了意义。现在投食和投蝇都要花钱，钱就有了一条持续的出口�?
| 动作 | 单价 | 一次最�?|
| --- | --- | --- |
| 普通苹�?| $0.001 | 10 �?|
| 金苹�?| $0.01 | 10 �?|
| 投蝇 | $0.005 | 10 �?|

价格�?`CONFIG.market.prices`，和 `CONFIG.market.shop` **分开�?*：shop 那个数组�?**一次性道�?*（买了变「已拥有」，按钮从此禁用），这里�?*可重复消�?*的动�?—�?两者的 UI 状态机根本不是一回事，混在一个数组里迟早要写一�?if�?
**金苹果的 1.5 倍是「年龄推进」而不是「只加速体型」�?* 吃到金苹果时 `age` 跑得�?1.5 倍，
于是幼虫更快跨过 `pupateAt` / `emergeAt`、成虫更快长大变�?—�?�?*也老得�?*�?等于用三分之一条命换更重的身体。这是刻意的取舍，不是副作用：有了代价，
「什么时候喂金苹果」才变成一个决定，而不是无脑一直喂�?
实现上它和罐中果蝇的「时间膨胀」是同一套：**只乘 age 的累加，`lifespan` 全程不改**�?所以体�?/ 体重 / 价�?/ 死亡判定全都自动跟着走。全项目一共两行：

```
Larva.update:  this.age += dtMs * foodGrowthBonus(this.eating?.type)
Fly.update:    this.age += dtMs * foodGrowthBonus(this.feeding ? this.bait?.type : null)
```

�?两处读的都是**上一�?*的进食状�?—�?`age` �?update 开头累加，�?`eating` / `feeding`
要到同一帧的后面才重算。这不是将就：`world._updateFeeding` 统计「谁在吃」用的也�?同一份状态，两边口径一致才不会出现「看着在吃、结算说没吃」的错位�? 帧延迟看不出来�?
**为什么食物只能落在左上角�?* 和「产卵靠屏幕边缘」是同一条产品要求，不是生态取舍：
这是桌宠，苹果丢得满屏都是会挡住玩家干活。区域用**归一�?*坐标存在 `CONFIG.food.zone`
（横 1%~22%、纵 1%~40%），换分辨率不会变形。想知道它到底在哪儿，把 `zone.show`
改成 `true`，屏幕上会画出一个金色虚线框 —�?默认不显示，因为一个长期挂着的大方框
本身也挡桌面�?
�?投放区的像素换算只走 `foodZoneRect()` 一个函数，world 撒点�?UI 画框都用它�?各算各的话迟早会出现「框画在这儿、苹果落在那儿」�?
**没有破产保底 ⚠�?* 早先有一版是「场�?0 只成虫时投蝇免费」，现在**取消�?*�?所以要清楚它的后果：钱花光之后再断了代（没有成虫可�?�?没有收入），
就真的什么都做不了了，只能点「重置」重开一局�?
这是有意接受的取舍：留一条「没钱也能白拿果蝇」的后路，等于把「要不要留一手钱�?这个决定整个抹掉，而那正是把钱做成资源的意义所在。模拟器里有一�?*反向**断言守着�?—�?不然哪天有人觉得「这样会卡死玩家」又好心加回来，没有任何测试会拦�?
（顺带一提：食物和果蝇都是按**实际成交的数�?*收费的，钱不够就一件都不发、一分不扣，
撞上上限没放下的部分当场退钱。所以「点下去什么都没发生」不会发生。）

**撞上限现在会说话了�?* 早先投食 / 投蝇撞到上限�?*静默失败**的：`dropFoods` 返回 null�?而按钮把返回值丢掉，于是「点了没反应」，看着像按钮坏了。现在面板上的操作要么成功�?要么在提示条上说清楚为什么没成（「食物放不下了」「果蝇到上限了」）�?
**�?10 个但只放得下 3 个时，只�?3 份的钱�?* 先把整笔扣掉、再按实际放下的份数
把差价退回来 —�?比「先算能放几个再扣」少一次重复的上限推算，上限只�?`dropFoods`
里判一次，不会出现两处算法漂移�?
�?顺带挡住了一个很容易忽略的浮点问题：`0.001 × 10` �?IEEE754 里是
`0.010000000000000002`。这个尾巴会一路渗�?`world.money`，攒够十几次就漂�?一个显示得出来的误差。所以所有「单�?× 数量」都要过 `bulkPrice()` �?round�?�?`formatMoney` 里那�?round 是同一个理由，只是位置更靠前�?
### 关于统计�?
主面板那一�?*只有「存活」和「死亡」两个数**，其余（成虫 / 幼虫 / �?/ 食物 /
成虫总价值）收进右边那个三条杠里，默认收起�?
理由不是「简洁」这种好听的话，是这两个数和其他�?*回答的问题不一�?*�?存活和死亡回答「现在还有多少活的」，是一眼要看到的；
分类明细回答「都是些什么、值多少钱」，是想查的时候才看的�?面板一�?318px 宽，五个数挤在一行，结果就是主次全糊在一起�?
**成虫总价�?*（`world.counts.value`）是屏幕上所有成虫售价之�?—�?也就是「现在把它们全拖去卖掉能拿多少钱」。它比只数更能说明这局养得怎么样：
40 只瘦蝇也顶不上一只巨兽。⚠ �?*不含罐子里的**，那些不�?`world.flies` 里，
得先放逐才能卖�?
三条杠是�?CSS 画的三条线（和垃圾桶 / 金币 / 捐款小罐子同一套做法）�?自检里那条断言量的�?*看不看得�?*（`getComputedStyle().display`），不是类名 —�?类名对了�?CSS 没生效的话，那排数字照样糊在主面板上，而类名断言全绿�?
### 关于烤制：尸体是会掉价的

一条完整的链路�?*苍蝇拍（F）拍死一�?�?地上留下带售价的尸体 �?拿打火机/喷火�?*碰到**它就烤熟（或塞进烤炉）→ 戴手套拖进出售区卖钱**�?
**打火机和喷火枪是「接触即烤」，不是「按�?N 秒」�?* 原设计要按住 5 �?/ 3 秒，
实机手感很差：满屏尸体时得一只一只按住等，而且烤到一半手滑挪开就白烤（进度会归零重来）�?改成碰到就熟之后�?*按住鼠标扫过去一片就全熟�?* —�?因为 `_useTool()` 在按住时每帧都跑，每帧都在问「指针底下那具尸体熟没熟」�?两档现在只差倍率（�?.2 / ×1.5）�?
�?顺手删掉的东西：`ui.roastHold` / `roastTarget` / `roastHinted` 三个字段�?`render.js` �?`drawRoastProgress()`（进度环）和调用点、`app.js` 里的 `view.roast`�?**�?`CONFIG.market.roastChain` �?`roastMs` 保留着** —�?烤炉（lv3）还在用它当烘烤时长，那是真的「放进去�?8 秒」，和接触即烤是两码事�?
拍死的那一刻，那只果蝇�?*售价快照**抄进尸体（`world.addRemains(..., fly)`）�?不抄的话尸体就只是一堆擦掉完事的垃圾，整条烤制链没有入口�?
**尸体随时间掉�?*：前 **5 分钟价格完全不变**（给足时间去拿打火机、凑一炉）�?之后�?**15 分钟里从 1 倍线性掉�?0.1 �?*，到底就停住�?�?不归零是刻意�?—�?留一点残值，玩家清屏时顺手卖掉也算没白干�?归零的话那些陈年尸体就只剩「拿抹布擦掉」一条路了�?掉价做成 `Remains.decayFactor` 这个 getter（由 age 推导），不存字段 —�?�?`rot` 同一个理由：存字段的话，任何没走�?`update()` 的路径都会让它停在旧值上�?
**倍率每具只吃一�?*（`Remains.roasted` 是单向的，`world.roast()` 对烤过的直接返回 false）�?允许反复烤的话，把一只养到最大再拍死、用最高档反复刷，
价格会指数膨胀，出售区那套「体重换钱」的定价就失去意义了�?想卖更贵，正路是**升级工具**（�?.2 �?×1.5 �?×1.8）�?
�?**只有 corpse 能烤能卖，汁渍（stain）不�?* —�?它是拍击溅出来的，不是一具身体，
`value` �?0。`Remains.roastable` 就是这个判断，`_corpseAt()` �?`sellFly()`
都走它，所以「指针在满地汁渍上一直抓到东西」这种情况不会发生�?
**烤炉为什么是容器而不是第三个工具�?* 前两档是「选中工具 �?碰地上的尸体」，
烤炉则是「手套抓最�?5 �?*活成�?*放进�?�?8 秒后一起出炉」—�?它吃的是活的，产出的是烤好的尸体。装满了**自动开�?*，没有再单独做一�?「开始」按钮：加了的话就得额外想清楚「放�?3 只能不能烤�?「烤到一半还能不能加」，自动开烤把这些边界问题一次消掉�?
�?烤炉里果蝇的时长�? 秒）**存在 `market.roastChain` �?lv3 那一条上**�?不在 `CONFIG.roast.oven` �?—�?它和「�?.8」�?16」是同一档的三个属性�?早先 `Oven.startRoast()` 去读 `CONFIG.roast.oven.roastMs`，而那个键**根本不存�?*�?于是倒计时变�?`NaN`、`NaN > 0` 为假、炉�?*一帧都�?tick �?*…�?
### 关于玻璃罐：观察模式也能拖，代价是什�?
罐子原来必须戴手套才能拖。现�?*观察模式下直接就能拖**（`_grabbableAt` �?罐子判定被提到了手套闸门**之前**）�?
�?代价要说清楚：窗口平时是**鼠标穿�?*的，指针停到罐子矩形里时窗口会接管鼠标，
于是**罐子底下的桌面点击会被吃�?*。单个罐�?194×246 �?屏幕�?2.3%，最�?4 个�?这和工具栏面板（常驻、面积更大）是同一类代价，不是新类别的�?
四条约束缺一不可，动之前先看清楚�?
1. **判定区一个像素都不许外扩�?* `_overPanel` / `_overDonate` / `_overJarWindow`
   都写�?±4px 图手感，`_overGrabbable` **不能�?* —�?外扩多少就多吞多少点击�?2. **也绝不要加「悬�?N 毫秒才接管」的防抖�?* 穿透时只有 mousemove 会被转发进来
   （`main.js` �?`setIgnoreMouseEvents(passThrough, { forward: true })`），
   按下是收不到�?—�?接管一旦晚于按下，那一下就穿到桌面，拖动直接失效�?3. **只放开玻璃罐�?* 食物 / 蛹壳 / 成虫一律照旧要手套。它们小、数量多�?   成虫还满屏乱飞，一旦在观察模式放开，整块屏幕会被几十个判定区打成筛子�?4. �?**罐子判定必须带工具条�?*：`canGrabJar = tool === 'none' || tool === 'glove'`�?   这一条是后来补的，补之前是个�?bug —�?罐子判定**什么工具都放行**�?   于是拿着**任何**工具点罐子都是在拖罐子，两个后果�?   **压在罐子上的虫永远点不开**（罐子先返回了，压根轮不到「查看」）�?   以及拿着苍蝇拍点罐子不挥拍。观察模式和手套**都要留在白名单里** —�?   删掉观察模式那条，「观察模式也能拖罐子」这个功能就没了；删掉手套更不用说�?
### 关于「查看」工具：为什么最后做成了专用工具

数据面板经历过三代：**悬停 1 秒自动弹 �?观察模式点一�?�?查看工具（V�?*�?最后这次改动的理由是前两代的定位自相矛盾：

- 观察模式的定位是�?*纯看不打�?*」，而「点一下能看数据」要求窗口在那只虫头�?  **接管鼠标、吞掉桌面点�?*（穿透时只有先接管、才收得到按下）。两件事是冲突的�?- 更硬的一条：罐子的判定排在所有工具之前（见上一节第 4 条）�?  所以观察模式下**压在罐子上的虫永远点不开**�?- 当时为了把「吞点击」的代价压小，还额外加了一条「只接管停着 / 在地上的虫，
  飞行中的不管」—�?那又让「追着飞虫点」点不开�?
换成专用工具之后这三条一起消掉了：拿着工具时整块屏幕本来就是操作区（和拿拍子�?拿网一样），所�?*飞行中的虫也能点**，`_creatureAt` 里那条限制已经删掉�?
**�?幼虫点不开�?* 卡片在幼虫身上几乎没东西可填，而蛆**满天都是**
（上�?45 只，烦人模式还要 ×50）—�?点一下弹一张卡片出来会把屏幕糊满�?所以幼虫那条路是从**根上删掉�?*，不是靠 CSS 藏起来：

- `_creatureAt` 不再遍历 `world.larvae`（它只返回成虫）
- `_showInspect` 里的 `isLarva` 分支、`.inspect.larva-card` 那一整块 CSS�?  `.gene-badge.carrier`（「幼虫带着但没显形」的基因标记）全部删�?- 自检里那条断言的方�?*反过来了**：从「幼虫也有面板」改�?  「点上去什么都不该发生」，而且三样都查 —�?`_inspectableAt` 返回 null�?  `_overInspectable()` �?false�?*不吞桌面点击**）、`openInspect(null)` 不开卡�?  还额外查了一条「成虫和幼虫叠在同一点上时必须点到成虫」，
  用来堵住「先收成虫、不够再收幼虫」这种写�?另外 `_updateStartle` �?`'inspect'` �?`'none'` 并列 —�?查看这一�?*不惊飞果�?*�?否则鼠标一动目标全跑了，一个走位就把要看的那只赶走�?
### 关于设置 / 捐款按钮：一个「同一次点击里开了又关」的�?�?
这两颗按钮曾�?*完全点不开**（设置卡和二维码都弹不出来），而整套自检全绿 —�?因为当时所有断言都是直接�?`setSettingsOpen(true)`，从来没走点击那条路�?
真正�?bug：两颗按钮里都套了一�?*铺满整颗按钮**的图�?span
（`.gear-icon` / `.jar-icon`），玩家实际点到的是 **span**，不�?button�?于是同一�?click 事件里发生了两件事：

1. 按钮自己的监听器先跑 �?`setDonateOpen(true)`，卡片打开
2. 事件冒泡�?`#panel` �?面板的「点别处就关掉」逻辑�?`e.target !== btnDonate`
   �?span 显然不等�?button �?**把刚打开的卡片又关掉**

净效果就是「点了没反应」，而且卡片其实闪开了一下，只是同一帧内又被关了�?极难从现象联想到是这里�?
�?**判「点在不在按钮上」必须用 `closest` 往上找**，不能比 `e.target` 和按钮本身�?现在写成 `e.target.closest('#btn-donate')`�?
自检那一节也因此必须**往 span 上派发一个会冒泡�?click**（`clickInner`），
直接把整条冒泡链路走一�?—�?�?button 是测不出来的�?
### 关于点击看数据面板：两个坑，别再踩回�?
数据面板（`#inspect`）最初是**悬停 1 秒自动弹**，上一版改成观察模式点一下，
现在�?*查看工具（V）点一�?*（为什么又改，见上一节）。两处不显眼但要命的地方
（这两条从第一版起就没变过，换触发方式不影响）�?
�?**一、`_updateInteractive()` 必须每帧调，不能只在 mousemove 里�?*

窗口默认穿透，`_updateInteractive` 决定要不要解除穿�?—�?而它原来只在
`mousemove` 里被调用。那对玻璃罐够用（罐子不会动），但对**会走的虫**两个方向都坏�?
- 鼠标不动、虫自己走到指针底下 �?没有 mousemove �?窗口仍然穿�?�?  那一下点击直接落到桌面上，「点开面板」静默失效�?  而这恰恰是这个游戏的常态：虫一直在动，玩家的手是停着�?- 虫走开了、鼠标不�?�?窗口**继续**吞桌面点击，直到玩家晃一下鼠�?
现在 `UI.update()` 末尾会调一次。每帧调不会刷爆 IPC —�?`_updateInteractive` 里有 `need === this.interactive` 的相等守卫，
只有状态真的翻转才�?`setInteractive`�?
�?**二、卡片必须「钉住」（`inspectPinned`），否则它活不过一帧�?*

`UI.update()` 每帧都跑 `_updateInspect()`，而它看到「指针底下没虫」就�?把卡片关掉。点开的卡片下一秒就被自己关�?—�?表现是「点了没反应」，
而代码看起来完全正常�?
另外一点：

- 判定半径�?*迟滞**（进 16px / �?28px）。虫是动的，
  卡在边界上会让穿透状态每帧翻转，光标和点击行为都会抖

（这里原本还写着「飞行中的虫不接管鼠标」。那条在查看工具那一�?*已经删掉**�?—�?理由见「关于『查看』工具」那一节。要是看到哪份文档还在讲它，那份文档过期了。）

面板**只显示，永不接管鼠标**（`pointer-events: none`）。接管鼠标的�?「指针底下有没有能点开的虫」这�?*另外**的谓词（`_inspectableAt`）—�?两者别合并，合并之后卡片一显示就会开始吞桌面点击�?（`_inspectableAt` 现在**只认成虫**，幼虫不会被它选中，见上一节。）

顺带，`_overGrabbable()` 走的�?`_grabbableAt() !== null` 而不�?`_jarAt(...) !== null`�?让「能拖什么」和「为谁接管鼠标」共�?*同一�?*真值来源，将来再放开什么会自动跟上�?
### 关于挥手惊蝇：为什么只在指针附�?
鼠标甩得越快�?*指针附近**的自由成虫逃得越�?—�?模拟「一只真果蝇躲开人手」�?两个因素相乘，缺一不可�?
- **距离**：指针正下方是满额，�?`startleRadius`�?90px）处降到 0
- **速度**：手甩得越快越慌，`startleFullSpeed`�?700px/s）封�?
只按距离算的话，手停在果蝇旁边不动也会让它们一直发疯；只按速度算的话，
屏幕另一头甩手会把全场点着。倍率封顶 `startleMaxMul`�?.5 倍）�?强度本身�?*快速回�?*（`startleDecay`�?.3 秒）—�?手停下了果蝇还会窜一小会儿，
像真的躲开了一只刚挥过来的手；没有这一段的话，鼠标一停它们就瞬间恢复悠闲�?看着像按住了暂停键�?
�?**观察模式（Esc）整个关�?*，连 `world.startle` 也一起清零�?那一档的定位是「纯看不打扰」——鼠标扫过去炸开一屏果蝇的话，观察模式就没法看了�?关的时候必�?*�?0** 而不是直�?return，否则切进观察模式的那一刻，
世界上海留着上一帧的强度�?
�?**只有自由飞的成虫受影响�?* 罐中果蝇�?`updateJarred`、烤炉里的走
`_updateOvens`，两条路都不经过 `_applyStartle()`，所以天然碰不到 —�?被关着的东西不该对外面的手有反应。代价是「进�?进炉前身上带着的倍率」会留下来，
所�?`Jar.admit` / `Oven.admit` 里要显式清回 1�?
`startleMul` �?`speedScale` 一样乘�?*消费�?*（`_walk` �?speed�?`_fly` �?wantSpeed），不在 `targetSpeed` 的赋值处 —�?赋值点有三四处�?漏一处就会「平时躲得开、某些时候突然不动了」，而那�?bug 只在挥手时出现，
极难复现�?
### 关于抢食与饿�?
一份食物能同时供几只幼虫进食，**按体积算**：`Food.maxEaters = size × eatersPerSize`�?果子现在�?24~200px，所以这个上限是 48~400 �?—�?早先这是个全局常数 6�?无论苹果多大都只能挤 6 只�?
�?**正常模式下这条几乎永远触发不�?*：场上幼虫上限才 45 只，而最小的果子
也容得下 48 只。（95% 的果子都�?24~30px 的正常尺寸，但座位是�?`size × 2`
算的，所以哪怕最小的那份也刚好够全场幼虫坐下。）真正会卡住的�?*烦人模式**，以�?`sim` 里那个把
`eatersPerSize` 临时压小的挤食测试�?
抢不到名额的幼虫**不会吃到任何营养**（`world._updateFeeding` 里按数组顺序
让前 N 只真的消耗），而且它们**不会赖在果子�?*�?
�?后面这半句是**单独一次修�?*，别把它和上限混为一谈。早先只做了配额判定�?超额的幼虫拿不到营养，但位置逻辑照旧把它们钉在食物上（`eating` �?null�?身体却叠在果子上）—�?玩家看到的还是一坨糊着，于是「上限到底有没有生效�?从画面上根本看不出来。修法是 `Larva.update` 里那�?`seated` 闸门�?没座位就不进 `eating`，直接掉进「游荡」分支散开�?*并且这份食物不再写进
`scentSpot`** —�?不写的话它会一直朝一个进不去的地方爬，卡在果子边缘原地打转�?等有空位了，下一次「闻」（间隔只有 0.4~1.6 秒）自然会把它叫回来�?
于是「吃没吃上」和「贴没贴在果子上」仍然是两回事：

- `l.eating` —�?位置判据，趴上了就是�?- `l.ateLastTick` —�?**结算判据**，由 `_updateFeeding` 每轮重写�?  只有真的抢到名额才为 true
- 两者之间还有一道迟滞：`ateLastTick` 为真就直接续座（位子已经归它），
  免得边缘那几只挤进挤出、每帧横�?
**饥饿只看后一个�?* 出生时每只抽一�?`starveMin ~ starveMax`�?0~18 分钟），
`hunger` 一直涨到超过它就饿死、留下一具蛆的尸体。区间而不是定值：
定值的话同一窝会在同一秒集体倒下，看起来像程序在清理，而不是「它们各自没吃上饭」�?
�?**蛹期不累计饥�?*，代码里那一句放在蛹分支**之后**就是干这个的�?蛹本来就不吃东西（`update` �?pupateAt 那个分支直接�?`eating` 清空），
照常累计的话每一只蛹都会在羽化前饿死，而症状是「蛹全都活不到成虫」�?
�?**这个机制在正常模式下基本用不�?*，是**有意**的：全场最�?45 只幼虫，
而一颗果子就装得�?38~60 �?—�?3 份食物约 117~180 个位子对 45 只�?实测�?5 只幼�?/ 3 份食物，�?90 秒取均值）**45 只全部吃得上�? 只游�?*�?
真正会卡住的�?*烦人模式**（幼虫上�?×50，另�?800 的总闸）—�?那时抢食和饿死才是主线。所以这条上�?*不是摆设，但也别指望在正常模式看见它**�?要验证它，得�?`CONFIG.food.eatersPerSize` 临时压小（`tools/simulate.js`
的「挤不进去的幼虫要让位」一节就是这么做的：把系数压�?0.25、一份随机苹�?（size 20~30 �?上限 5~7），30 只幼虫里只有 5~7 只进食，其余 16~17 只爬到果子外面去）�?这一节同时钉了两个方�?—�?既查「有没有超员」，也查「超额的是不是真的走开了」，
因为早先只做了前半句�?
### 关于基因突变

四种可遗传的变异�?*遗传模型是按现实来的**，不是「每次出生重新抽奖」：

- **新发**：每个卵在产下时独立骰一次，概率就是每种突变�?`chance`
- **遗传**：父母各带哪些突变，各有 `mutation.inheritChance`�?0%）传给这个卵

所以一只果蝇的基因 = 母亲传的 �?父亲传的 �?自己新发的。突变会在谱系里**扩散**�?而不是每代重新开�?—�?这正是「基因」这个词能否成立的分界线�?
| 突变 | 概率 | 见效阶段 | 效果 |
| --- | --- | --- | --- |
| **疯狂** | 2.9% | 幼虫 + 成虫 | 狂躁，定期攻击附近的同伴。伤害：幼虫 1 点，成虫 1~3 点�?*寿命砍半** |
| **点石成金** | 3% | 成虫 | 通体金色 + 闪光。自身价�?×1.3�?*附近**的成虫价�?×1.1（不叠加�?|
| **石化** | 2% | 成虫 | 石灰�?+ 颗粒材质�?*失去飞行能力**，体�?×1.5，被拖着走时明显发沉 |
| **结晶** | 1.1% | 幼虫 + 成虫 | 全身透明、只剩一圈流动的炫彩描边，价�?×2 |

> �?**结晶的描边粗细，幼虫和成虫必须一�?*（`render.js` 里两处的线宽系数都是
> `size × 0.11`）。成虫原来写的是 `0.055` —�?正好一半，配上一�?19px 的蝇
> 就只�?**1px �?*，而它还在�?300~1700px/s 飞。用户报�?> 「结晶成虫好像没有特殊效果」就是这个：效果全在，只是细到看不见�?> 幼虫那边多个 `/0.85`，是因为 `drawLarva` 有一�?`ctx.scale(1, 0.85)`
> 会把描边一起压扁，得补偿回去；成虫没有那层变换，直接用系数�?>
> 自检里那�?*查像�?*的断言盯着三件事：身体的平均不透明度（全透明）�?> 高饱和像素的色相铺开多少度（炫彩）、以�?*描边占多大比�?*（够不够粗）�?> 只查色相是不够的 —�?线宽调回一根头发丝也照样通过�?
#### �?四个数字相对真实突变率放大了七八个数量级

真实的突变率大约�?1e-8 / 碱基 / 代。照那个来，玩家一辈子见不到一次变异�?这里要的手感是「养几代就能看见」，量级上更接近**人工诱变实验**而不是自然演化�?这是刻意的游戏化，不是算错了�?
#### �?疯狂必须攻击同类，否则种群会被玩�?
如果疯狂只咬正常个体，带这个基因的谱系就有绝对生存优势（自己不受伤、还能清掉竞争者）�?几代之内会把种群里其他果蝇杀�?—�?玩家会发现自己的存档「自己没了」�?所以攻击目标里**包含其他疯狂个体**：自相残杀，自己压住自己�?
这不是为了平衡硬加的。现实里有害等位基因被自然选择压住，是同一个道理�?
#### �?疯狂蝇寿命砍半（`lifespanMul`�?
成虫乘在 `lifespan` 上（16~25 �?�?8~12.5 分）�?*幼虫没有「寿命」这条线** —�?它要么化蛹要么饿死，饿死是唯一的死亡时钟，所以减半落�?`starveAfter` �?�?0~18 �?�?5~9 分）。两处用同一个配置项，改一处就够�?
这也是「狂躁」的代价：光会咬人没有代价的话，这个基因就是纯收益�?和「攻击同类」是同一个方向的约束 —�?都让它更难在种群里站稳�?
�?别把这一条做�?`lifespan` �?getter。它是存下来的字段，
`growth` / `hpMax` / 死亡判据 / 罐中列表全都直接读它 —�?做成 getter 就等于把「寿命」拆成两套，�?`age + wounds >= lifespan`
那个死亡判据会和三处显示对不上�?
#### �?疯狂的发作间隔调过两次，别往小里�?
最初取的是 6~14 秒。跑 `sim` 量出来的后果是：开局 4 只里只要�?1 只带疯狂
（概率约 11%），它会在十分钟内把另外 3 只全部咬死，再顺手把幼虫压得长不到蛹�?—�?**整局�?40 分钟内灭绝，而且没有任何提示**�?
现在�?16~36 秒（约每分钟 2 口）。看得出来「这只很凶」，但不会把种群一次清空�?
#### �?生命值用 `wounds`（伤），不是推进 `age`

成虫的生命�?*就是剩余寿命**（分钟）：出生时 16~25 点，随老化自然下降，归零即寿终�?伤害 1~3 点的意思就是「少�?1~3 分钟」。幼虫固�?1~2 点�?
伤记在单独的 `wounds` 字段里，死亡判据�?`age + wounds >= lifespan`�?**不能直接把伤害加�?`age`** —�?`growth = age / lifespan` 会跟着涨，
于是 `weight` �?`value` 一起涨：被咬一口反�?*变重变贵**，是个反向激励�?
�?幼虫最�?2 点血，而成虫一口是 1~3 �?—�?所�?*每一口都必然打死一只幼�?*�?这是「成�?1~3 / 幼虫固定 1~2」这两套数值直接推出来的结果，不是 bug�?
#### �?石化的「鼠标变慢」做不到字面意�?
原话是「玩家拖动此虫时鼠标速度会强行变慢」�?*那做不到** —�?我们只能控制这扇窗口里画什么，改不了操作系统的光标速度�?真去劫持光标位置（`setPosition`）只会把它变成一块不受控的砖头�?
所以落点改成「被拖的虫落后于指针」：`_dragTo` 对石化蝇做阻尼插值而不是瞬间吸附�?手感上是同一件事（拽一块石头走），而且完全是窗口内部的、可逆的�?
#### �?石化 ×1.5 体重会顺带把售价也抬 1.5 �?
因为 `value = priceOf(weight)` 是游戏本来就有的换算。一增（更值钱）一减（不能飞）�?这是有意的。不想这样的话，就在 `Fly.value` 里用去石化的体重�?
#### �?突变集合必须�?*普通数�?*，不能是 `Set`

`snapshot()` 只认「原始�?/ Array / 纯对象」三种形态，`Set` 会从三条分支中间
直接掉下�?—�?字段静默不进存档�?*每次读档突变全部消失，不报错也不崩溃**�?（`entities.js` 末尾那段「黑名单就是防这个」的注释挡不�?`Set`，它只防漏写字段。）

#### �?新发突变只在**产卵�?*�?
不要�?`Fly` / `Larva` 的构造函数里骰。那�?`sim` �?「一只普通果蝇的售价 = 体重 × 单价」那条断言会随机红（每次约 4%），
而且看起来像价值公式坏了。同理，花钱「投放」出来的果蝇**不骰** —�?否则买蝇就成了买彩票，对售价是正期望，可以无限刷钱�?
唯一的例外是**开局那几�?*：它们走的是「生物学的出生」那条路�?不骰的话玩家要等整整一代（20 多分钟）才可能见到第一个变异�?
### 关于受惊：慢速靠近不惊，快甩才惊

�?**`startleWakeSpeed`�?00px/s）是一条死区线：慢于它，果蝇完全不受惊**
—�?power 直接�?0，连速度都不加�?
这是「玩家得抓得住它们」那条线。拿网罩、拿手套拎、拿拍子拍，都要先把�?**稳稳�?*移过去。没有这个死区的话，鼠标一动就惊飞，瞄准这件事根本做不�?（第一次实测就是这个：手刚抬起来还没到，果蝇已经全跑光了）�?
�?**死区必须�?`ui._updateStartle` 里减掉，不能放到 world 那边按倍率截断�?*
后者的话，慢速移动仍然会得到一个略大于 1 的倍率，而玩家瞄准时手会
一直小幅抖�?—�?等于全程给果蝇加速�?
超过死区之后线性升�?`startleFullSpeed`�?700px/s）到顶，倍率封顶 ×3.5�?
### 关于受惊：必须真的飞走，不只是「飞得快一点�?
�?这里踩过一次。第一版只�?`startleMul` 乘到速度上，结果**悬停中的�?根本不会�?* —�?`_fly` 里是 `hoverTimer > 0 ? 0 : targetSpeed`�?目标速度�?0，乘多少倍都还是 0。趴在地上走的那批也只是走快一点，不会起飞�?玩家看到的正是「手从它身上扫过去，它还在原地悬着」�?
所以受惊必须是**行为**上的改变�?
| 原本 | 受惊�?|
| --- | --- |
| 悬停中（`hoverTimer > 0`�?| 踢出悬停 |
| 停顿中（`pausing`�?| 取消停顿 |
| 在地上走（`mode === 'walk'`�?| 强制转成飞行 |
| 朝任意方�?| �?*背离指针**的方向�?|

转向�?`angleLerp` 混（`startleTurn`）而不是直接赋值：硬掰会让满屏果蝇
在同一帧齐刷刷转向，像被同一个磁场推开；混一下各自保留一点原路线�?才像各自在躲。同时把 `dartTimer` 顶到至少 `startleDartHold`�?免得刚转过去就被窜动节律重抽掉�?
### 关于蛆的两种长相

孵化时按 `larva.translucentChance`�?*两成**）抽一次，抽中的长成「外圈微透�?中间实」，其余是同色实心。这�?*纯外�?*：不代表健康、年龄、稀有度�?也不影响任何数�?—�?只是让一窝蛆有点个体差异，整批一模一样最容易看出
「这是程序画的」�?
�?它在**构造函�?*里抽、抽完一辈子不变。放到渲染时每帧随机的话�?蛆会在透与不透之间闪，而且存档读回来会变成另一个样子�?（`snapshot` 是反黑名单，所以这个新字段自动进存档。）

> �?`translucentChance` �?`CONFIG.**larva**` 的字段，不是 `visual` 的�?> 放错地方**不会报错**：`Math.random() < undefined` 恒为 false�?> 表现只是「一条透明的都没有」；而断言�?`Math.abs(share - undefined) > 0.03`
> 得到 `NaN > 0.03` = false�?*连测试都抓不�?*�?> 现在测试里先单查一遍这个值是不是 0~1 之间的数，就是为了堵这个�?
### 关于烦人模式：为什么食物不跟着 ×50

设置卡里可以切到**烦人模式**，生态上�?×50�?
| | 正常 | 烦人 |
| --- | --- | --- |
| 成虫 | 40 | 2000 |
| 幼虫 | 45 | 2250 |
| �?| 40 | 2000 |
| **全场生命体总数** | 不设�?| **800 封顶** |

三条设计上的讲究，都是踩出来的：

1. **食物和残留物的上限不跟着乘�?* 它们不是生物，而且是真正的性能杀�?—�?   每份食物都要跑轮廓、霉斑、招蝇判定。跟着 ×50 变成 1500 份的话，
   帧率会直接打穿，而且**不会报错、不会崩**，只是越来越卡，是最难归因的那种�?2. **三项�?×50 是不够的**，必须再加一�?*总数�?*�?00）。三个上限会同时顶到�?   而它们各自又都在自己那套循环里跑（交配、羽化、孵化）�?   加起�?6250 —�?那不是「烦人」是「卡死」�?3. **切回正常模式时，场上多出来的不杀�?* 上限只拦**新增**，不管已经存在的�?   切一下模式就凭空蒸发几百只果蝇，比留着它们更让人困惑�?   想立刻回到清静状态，点重置�?
模式做成 `world.settings`（进存档）而不是在切换时改 `CONFIG`�?`CONFIG` �?*只读的调参表**、全局共享，往它上面写运行时状态的话，
重置、读档、以及任何「恢复初始值」的地方都得记着还原，漏一处就会留�?一个改不回去的世界。老存档没�?`settings` 字段，`Object.assign` 到默认对象上�?自然落回正常模式�?
### 关于苍蝇拍：杀伤点在拍面上

拍子�?*斜着�?*的：指针那一点是握把末端，拍面在指针�?*左上�?*�?虚线圈、杀伤判定、汁渍和挥拍特效全都落在**拍面**上，不在指针上�?
�?这几个点必须�?`CONFIG` 里那个纯函数 `swatterHeadAt()` 算出来，
渲染�?`ui._useTool()` 共用同一份结果。各算各的话�?偏差的症状是「明明看着拍中了，果蝇却没死」—�?凭手感几乎查不出来�?自检里那条断言不直接调 `swatterHeadAt`（那样测的是「函数等于它自己」）�?而是真的挥一拍、量 `world.swings` 里记下的落点离指针多远�?
调的三个数是 `tools.swatRadius` / `swatAngle` / `swatReachFactor`�?
### 关于捐款入口

面板右下角那个描线会流动的小罐子（纯 CSS 画的，和 `.bin`/`.coin` 同一套做法）�?点开�?*屏幕正中**弹出一张卡片：支付宝收款码 + 「自愿捐款，理性消费哦」�?二维码是 `renderer/assets/alipay-qr.png`，靠 `package.json` �?`files` �?`renderer/**/*` 进包�?
**它和游戏里的一切都不挂�?* —�?不捐款不会少任何东西，捐了也不会多任何东西�?所以它刻意做得不起眼：一�?20px 的小图标，不弹窗、不提醒、不挡路�?
位置**钉死�?CSS �?*：`.donate-pop` �?`left/top: 50%` + `translate(-50%, -50%)`�?没有任何 JS 摆位，也没有每帧校位。早先的版本是「贴在面板正上方」�?`left/top` �?`ui._placeDonate()` 每帧现算 —�?桌面版上没问题，
�?*面板一拖到屏幕顶端或右边缘，卡片就会被 clamp 到边�?*，和面板挤在一起，
二维码顶出可视区，看着像图没加载出来。改成居中之后，位置和面板在哪完全无关�?
摆这个卡片连踩了三个坑，写下来免得以后又绕回去：

1. **不能放进 `.window` 里�?* 那扇窗有 `overflow: hidden`（用来裁圆角），
   卡片会被整个剪掉，一点都看不见�?2. **必须�?`.donate-pop` 一�?`z-index`（现在是 30）�?* 卡片居中之后和面�?   **是会重叠�?*（面板默认停在底部中央，窗口一矮就压上），而面板底色是
   `rgba(18,16,14,0.84)`（半透明）—�?让它盖在卡片上，二维码就从底下透出来糊成一片�?   30 压过 `.window`（`z-auto`）和悬停卡片�?20，绘制和命中就一致了�?   自检里有一条断言专门守着这个数，因为**漏写这一行不会有任何别的症状**�?3. **要挂�?`#hud` �?*（和 `.window` 并列），不能直接�?`body` 下�?   直接�?`body` 的那一版，卡片在这�?*透明无边框窗�?*里合成得不对 —�?   截图里糊成半透明的浅灰，�?DOM 里量到的样式对不上。挪�?`#hud` 这棵子树就正常了�?
�?代价是没有「点空白处关闭」（那需要一块全屏背板）。关法是 �?/ `Esc` /
再点一下小罐子 / 点面板别处。也正因为没有背板，�?*必须参与
`ui._updateInteractive()`** —�?但走的是命中判定（`_overDonate`）而不�?「开着就接管」：它只是屏幕正中一张小卡，不是模态，没必要开着就吃掉整块屏幕的点击�?`_overDonate` �?`_overPanel` 都是矩形判定，所以卡片和面板重叠时也不会互相盖掉�?
> **自检里量位置的断言必须�?`setDonateOpen(false)` 之前�?*
> `display: none` 的元�?`getBoundingClientRect()` 全是 0 —�?断言写在关闭之后
> 就变成「拿 0 和屏幕中心比」，只会永远通过。所以那一段先显式确认 rect 不是空的�?
> 这条和悬停卡片、食物投放区正好相反：那两个**必须保持穿�?*�?> 三条规则的区别只看一件事 —�?**上面有没有要点的东西**�?
### 怎么把某一只果蝇存进罐�?�?
**两条路，用途不一样：**

| | 怎么�?| 适合 |
| --- | --- | --- |
| **捕虫�?*（N�?| 点一下，半径内的成虫进最近的罐子 | 随手收一�?|
| **手套拖进�?* | 按住某一支成虫，拖到罐子上松�?| **只留那一只稀有的** |

�?**第二条路以前根本不存在�?* `_endDrag()` 只处理垃圾桶和出售区，罐子一直只能靠网进�?—�?而网是按半径一网打尽的，想「在一堆轻盈蝇里只留下那只巨兽」只能靠运气�?这就是「很难储存稀有的成虫」的真正原因，和时停没有关系（拖拽全程走的是 UI�?`world.paused` 管不着它，时停时照样能拖）�?
这条路径在自检里是**真的走一�?*的：摆罐�?�?抓蝇 �?把指针放到罐子上 �?�?`_endDrag()`
�?查它有没有真的进罐。只查「函数存在」是不够的，�?*分支整个漏写**恰恰是那种查法查不出来的�?
顺手加的：拎着成虫压到罐子上时，那个罐子会变成金色（盖�?+ 描线一起变）�?没有这个提示的话，「能不能放进去」只能靠松手试一次，而试错的代价是那只稀有的�?又被扔回屏幕上飞走了�?
**罐子满了**不会把果蝇弄�?—�?`putInJar` 返回 false，果蝇留在原地，提示条说「这个罐子满了」�?
### 养蝇人（商店 $2 �?再付 $5�?
替你干两件重复劳动。设计上有三处刻意的取舍�?
**�?自动投放**照价扣钱**，不是免费的�?*

买了养蝇人之后就免费投的话，玩家一定会把它设成金苹�?—�?而金苹果让成长快 1.5 倍，
等于�?$2 买断了无限个原本 $0.01 一个的东西。所以走的是和投放面�?*同一�?*
`buyFood` 路径：照价扣、放不下退钱、钱不够就这一次不投（下次有钱了自然会再投）�?
普通苹�?$0.001 一个，卖一只普通成虫就够投十几个，所以实际手感上几乎是免费的�?真正被约束住的只有金苹果�?
**�?出售的筛选条件只有两条，而且都和数据面板上看得见的东西一一对应�?*

�?*卖哪�?*」（价值档�? �?*含不含突�?*」。就这两条�?
> �?这里**曾经**还有第三条：一根「价�?≥」的对数价格滑条�?0.001 ~ $1000�?> 中点正好 $1），�?`world.keeper.minValue` �?`market.priceFromSlider` /
> `sliderFromPrice` 两个互为反函数的换算�?*整条删掉�?*（用户要求）�?> 删的时候四处一起删干净：DOM、`keeperOptions` 的上下限、`world.keeper` 上的
> 字段、`_keeperSell` 里那一处比较�?>
> 特别记一�?*为什么状态字段也必须�?*：只删界面不删状态的话，自动出售会被
> 一�?*看不见、也改不�?*的门槛拦着 —�?玩家只会觉得「开了自动出售怎么不卖」，
> 而任何「点了没反应」式的断言都抓不到它。`sim` 和自检里各留了一�?> **反向断言**（`'minValue' in world.keeper` 必须为假），就是防这个�?
反过来「卖掉不值钱的」（�?某个价）也说得�?—�?那是清垃圾、腾种群名额 —�?但和「养蝇人」这个名字给人的预期相反，所以没做。真想要「养到够贵就收割」，
把「卖哪档」设成对应的高档即可：果蝇越老越重越值钱，档位本身就是价格门槛�?
**�?筛的是「价值档」，基因是另一套，别混�?*

| | 是什�?| 取�?| 怎么来的 |
| --- | --- | --- | --- |
| `tier`（配置卡上的「卖哪档」） | **价值档** | 普�?/ 罕见 / 稀�?/ 极稀�?/ 超级稀�?| 由当前售价现算（`valueTierOf(f.value)`�?|
| `mutations` | **基因** | 疯狂 / 点石成金 / 石化 / 结晶 | 遗传 + 新发（见「基因突变」） |

一只「普通」档的蝇完全可以带着 **石化** 基因。所以这两行是正交的筛选条件，
配置卡上也是分开两行 —�?把它们当成一件事是这个功能最容易犯的错，
`sim` 里有一条断言专门钉着它�?
�?这里�?*踩过一次坑**：筛选原先比的是 `f.rarity`�?*体格 / 体重�?*），
而那和玩家在数据面板上看到的**价值档**不是一回事 —�?一只超重蝇具体落在「稀有」还是「极稀有」，要看它羽化时抽到的体重�?照体重档筛的话会出现「面板明明写着稀有，按稀有筛却筛不出来」�?现在两边共用 `valueTierOf()` 这一个来源�?
**另外两条没写在配置卡上、但要知道的�?*

- **罐里的和烤炉里的果蝇，自动出售碰不到�?* 它们不在 `world.flies` �?  （见「玻璃罐」那一节）。这是有意的：罐子里是你特意存的，炉子里是在烤的
- **正在产卵的母体不卖�?* 把她卖掉会在场上留下一窝永远产不完的卵�?  而玩家看到的是「刚才还在下蛋的那只突然没了�?
### 警报器（商店 $3�?
罐中果蝇寿命�?20% 亮橙光、剩 10% 亮红光，会呼吸，越快到点闪得越急�?
**只管罐子里的�?* 外面满屏飞的果蝇不点�?—�?那会变成一片闪烁的彩色点，
而且外面的果蝇本来就该自然生灭，不值得逐个盯。罐子才是「你特意存起来的那几只」，
提醒才有意义 —�?这也正是这个道具存在的理由：**罐中寿命是外面的两�?*�?活得久，也就更容易在你没注意的时候悄悄到头�?
阈值判断抽成了 `config.js` 的纯函数 `alarmLevel(fly)`�? / 1 / 2），
这样模拟器能**精确**测边�?—�?20.1% �?19.9% 该亮哪一档，靠肉眼看光环是分不出来的�?边界�?*�?*的（�?而不�?<）：写成 < 的话，正好卡�?20% 的那一帧不会亮�?而剩余寿命是连续变化的，这种「差一点点」在实机上就是「有时候不灵」�?
> 颜色�?`visual.alarmOrange` / `alarmRed`，阈值在 `market.shop` �?alarm 那一项�?
### 玻璃罐：盖子和尺寸是怎么来的

尺寸不是估的，是**逐像素量**出来的：把截图扫一遍、取红色描边的包围盒�?最新一版量到的�?**194×278 的整体包围盒**（原�?1920×1080），于是按它反推�?盖子占掉顶上 32px，剩下的 246 就是罐身；盖子宽度按原来的比例（0.69）跟着放大�?134�?
�?**量的是整体，不是罐身�?* 罐子纵向占的�?`height + capHeight` —�?只对着 `height` 调的话，实际画出来会比图上那个框高出一截盖子�?
**盖子在罐身上沿之上，不在罐身里面�?* 早先这里是一道画在罐身内部的「罐口横线」，
那读起来始终是「一个方框加一横」；真正让它像罐子的，是上面那个**比罐身窄一截的
实心脑袋**。这是这次改动里唯一一处「形状」上的变化，其余都只是换色�?
�?盖子探出罐身之外，这件事有两个连带后果，都得跟着改：

1. **夹边界要�?`jar.topHalfH`（罐身半�?+ 盖高），不能�?`halfH`�?*
   拖到屏幕最上面、或者窗�?resize 时，�?`halfH` 夹的话盖子会被顶出屏幕外�?   罐子看着像被削掉了脑袋。水平方向没这个问题（盖子比罐身窄）�?2. **容量小字要摆在盖子之�?*（`-hh - ch`）。还摆在 `-hh` 的话会被盖子整个压住�?   一个字都看不见�?
颜色：罐身描�?`jarRim` �?*半透明天蓝**（原来那版几乎是白的�?在白桌面上就是一圈灰白线，看不出是玻璃）；盖�?`jarCap` 是罐子上
**唯一一块不透明的东�?*，实心棕色�?
### 关于「罐中果蝇」小�?
它原来只是工具栏面板里的一块折叠区，后来才搬出来做成独立小窗。搬家的理由是：

- 面板一共才 **318px �?*，而一行要塞八个东西，本来就在硬撑�?- 4 个罐子最多能�?**32 �?*果蝇，一展开就把垃圾桶和提示条顶到看不见的地方�?
搬出来之后宽度能给到 **300px**（去掉内边距�?282px，那八个东西刚好排得下，
进度条独占剩下那 ~48px 的弹性空间），而且**默认收起、只留一条标题条** —�?标题条上就写着「罐中果�?7」，不展开也知道罐里有几只�?
**位置钉在右上�?*（`ui._placeJarWindow()` 第一次显示时给的默认值）�?左下有工具栏面板、正中是捐款卡片，右上角是唯一不会和它们打架的角落�?位置记在 `ui._jarPos` 里�?*不是�?DOM**：没有罐子时整扇窗是 `display: none`�?那时�?`offsetLeft` 之类全变�?0，读 DOM 的话下次出现就跳回左上角了�?
�?它和捐款卡片一样，**必须挂在 `#hud` �?*（不能放�?`.window` —�?那扇窗有 `overflow: hidden`，会把列表整个剪掉），并�?`z-index: 25`�?压过面板（`z-auto`），但低于捐款卡片的 30 —�?两者都可能停在屏幕中间�?而捐款卡片是玩家主动点开的一次性交互，优先级更高�?
�?�?*参与 `ui._updateInteractive()`**（走 `_overJarWindow` 的矩形命中）�?不参与的话，标题条和「出�?/ 放逐」按钮长得完全正常，只是点上去会穿到桌面上�?`_overJarWindow` 里那�?`.hidden` 检查不能省 —�?`display: none` 的元�?`getBoundingClientRect()` 会返回一�?**0×0、位�?(0,0)** 的矩形，
不查的话屏幕左上角会有一小块莫名其妙地吃掉桌面点击�?
�?拖标题条�?*把随后那一下点击吃�?*（`ui._jarSkipClick`）。面板的标题条上没有
click 处理，所以那边没这个问题；这边点一下是「展开 / 收起」，拖完位置顺手
把列表收起来会让人莫名其妙�?px 以内不算拖，免得手抖一下就让「点一下展开」失灵�?
### 关于玻璃罐的两个设计决定

**罐中果蝇不在 `world.flies` 里，而是挂在 `jar.flies` 上�?*
所以「隔离」是白送的：交配、苍蝇拍、生命周期结算全都只遍历 `world.flies`�?果蝇不在里面就自动不参与 —�?不需要在那些地方各塞一�?`if` 判断，也就不会漏掉某一处�?（漏掉的那种 bug 表现为「罐子里的果蝇莫名其妙死了」，很难查。）

代价是它们要单独更新、单独收尸、单独存档。这三件事集中在 `world._updateJars()`
�?`Jar` 类里，改动时别漏�?
**罐中果蝇�?`x` / `y` 是相对罐子中心的偏移，不是屏幕坐标�?*
渲染时要写成 `(jar.x + f.x, jar.y + f.y)`。这么存是为了拖动：
只要�?`jar.x / jar.y` 一个地方，罐里所有果蝇自动跟着走，
不会出现「拖动时果蝇掉队」，也不需要每帧同步一遍坐标�?�?直接�?`jar.flies[i].x` 拿到的是偏移，不是屏幕位置�?
**罐子是矩形，所以边界判定不能用「比半径」那一套�?*
`Jar` 暴露的是 `halfW / halfH`（外框）�?`innerHalfW / innerHalfH`（果蝇活动范围）�?涉及的地方有四处，改形状时都要跟着看：渲染�?`jarPath()`�?`ui._jarAt()` 的命中判定（点是否落在框内）、`ui._dragTo()` �?`world.resize()`
的边界钳制、以�?`Fly.updateJarred()` 里的四条边反射�?
### 蛆的「软身体」是怎么做出来的

**核心只有一句话：身体存在世界坐标里，不在头的局部坐标里�?*

`Larva.spine` 是一串世界坐标的点（`[0]` 是头）。每帧的规则只有一条：

```js
// 每个点被约束在「离前一个点恰好一个体节」的位置上，自己不主动移�?const k = (d - seg) / d
cur.x -= dx * k
cur.y -= dy * k
```

于是头往前走，尾巴是�?*�?*过去的，走的是头刚才走过的那条路径�?头转弯时，世界坐标的脊柱还留在原�?—�?换算回头的局部坐标（`bodyAxis()` 里那�?反向旋转）就自然弯了�?*「软」来自身体跟不上头，不来自身体自己摆�?*

�?这里有一个非常容易走错的方向：给每个点叠一�?`sin(相位 - t*波数)` 的横向偏移�?那样身体确实会扭，但整条�?*刚性绑在头的朝向上**�?—�?头一转，身体立刻跟着转，
看着还是一根硬棍子在平移（而且直行时会一直无谓地扭，很假）�?这个坑踩过一次，别往回改�?
`bodyAxis()` 把世界坐标的脊柱换算成渲染要的局部坐标，做成 `Larva` 上的方法是为�?**让自检也能拿到同一条曲�?*：头有没有钉在原点、体节有没有被拉伸、转弯时会不会弯�?都能在无头环境里断言，不用靠肉眼看画布。`render.js` 只是把它撑成左右两条轮廓�?连成一个闭合路径画出来�?
另外三个容易踩的点：

1. 轮廓�?*两端必须补半圆头**（`bodyOutline()` 里的 `cap()`）。只把左右两条侧�?   首尾直连的话，头尾各是一条垂直于身体的直线切�?—�?尾巴看上去像被削掉一块�?   �?这个 bug **不能靠调半径剖面�?*：剖面只决定「切面有多宽」，不决定「切成什么形状」�?   自检里有一条断言专门守它（量轮廓在身体方向上比脊柱末端鼓出去多少�?   圆头应当正好鼓出一个端点半径）�?2. 轮廓的法线要**按切线算**，不能直接沿 y 轴偏�?—�?   身体弯得厉害时那一段中轴接近竖直，横着撑会把轮廓挤变形�?3. 半径剖面必须平滑，出现台阶就又变回「分节」了�?
**�?`mixHex()` 可以链式调用，但入参必须是它能解析的颜色�?*
`mixHex()` 返回的是 `rgb(r,g,b)` 字符串，再喂回给它自己是完全正常的写�?（蛹就是「先混成褐色、再往深褐混一点」）。早先的解析只认 `#rrggbb`�?于是 `mixHex(mixHex(...), ...)` 会解析出 NaN、拼�?`'rgb(NaN,NaN,NaN)'` —�?而这个字符串交给 `fillStyle` 时会被画�?*静默忽略**：不报错、不抛异常，
只是继续沿用**上一个颜�?*。现象是「蛹突然变成半透明 / 变成别的颜色」，
而且换个绘制顺序现象还会跟着变，几乎没法从代码上看出来�?现在解析支持自己的输出格式，并且解析失败�?`console.error` + 返回刺眼的洋红，
不再静默�?
**「不分节」是靠画法实现的，不是靠参数�?*
早先身体是一串圆珠，每颗单独填充 + 单独描边，「能数出几节」就是那么来的�?现在是一条闭合路径一次性填充，中间没有任何描边 —�?`config.larva.bodySamples` 只是采样点数，改它不会让分节回来�?
### 关于描边 �?
**生命体一律不描边�?* 翅膀、卵、幼虫身体现在全是纯填充�?
> **例外一：蛹**。它是一枚实心硬壳，完全没有边界会糊在桌面上�?> 所以单独给它留了淡淡一层轮廓（`visual.pupaRim`）�?> 这不是把规则推翻了，而是「不描边」的目的（别让形状浮起来）在实心硬壳�?> 靠颜色达不到 —�?蛹和桌面的明度差不够�?>
> �?alpha 定在 0.46 而不是更低。曾经是 0.30，在浅色壁纸上基本看不见，等于没画�?>
> **例外二：结晶变异**。它的设定就是「全身透明、只剩描边」，
> 所以幼虫和成虫都会勾一圈炫彩轮廓（成虫�?`flySilhouette()`�?> 幼虫直接用现成的 `bodyOutline()`）。不勾的话，一�?alpha 0.14 的填�?> 在浅色桌面上基本等于不存在，这个变异在画面上就没了�?>
> �?**两道都要够粗才成立�?* 成虫那道原来是幼虫的**一�?*宽（1px），
> 结果就是「勾了等于没勾」—�?详见前面「基因突变」那一节的注释�?> 一处淡到看不见的描边和一处没画的描边，在玩家眼里没有区别�?>
> �?这是**目前唯一**一处「给生命体的外形勾边」。别拿它当先�?—�?> 当年正是「给每个填充都勾一圈」把这个文件画成贴纸的�?> 石化蝇那层暗边（`V._rim`）不算例外，它和蛹是同一类：灰／米白和桌面亮度太近�?
理由是描边和投影是同一回事 —�?都会把形状从桌面上「拎」起来、看着像贴纸，
和这里「东西就长在桌面上」的整体风格冲突（全场没有投影也是同一个理由）�?
但有两类 `stroke` **必须保留**，它们不是在勾勒轮廓，去掉等于删掉那个部件：

| 保留�?| 去掉会怎样 |
| --- | --- |
| 果蝇的腿、雌蝇腹部的环纹、胸背上的刚�?| 腿是线条画的，去掉就没有腿了 |
| **翅膀里的翅脉** | 画在翅膜**内部**的纹路，不是给翅膀勾边。去掉的话翅膀就退化成一块灰色色块（见下面「成虫体表材质」） |
| 苹果的果�?| 那是红苹果皮，不是描�?|
| 玻璃罐的边框和罐�?| 罐子填充只有 6% 透明度，看得见全靠那圈边�?—�?去掉就整个消�?|
| 苹果果肉那圈同色描边 | 它是用来把折线的尖角磨圆的，颜色和填充一样，视觉上不属于描边 |
| 蛹的轮廓（很淡） | 实心硬壳，没有边界会糊在桌面上（见上面那条例外） |
| 蛹壳轮廓上的那个缺口 | 那是成虫钻出来的洞，不是描边 |

**材质**：成虫和蛹身上各有一�?*很细的颗�?*（`render.js` �?`speckle()`），
让它读起来像个有壳的实体而不是一块纯色贴纸。颗粒的位置由各自的 `seed` 决定 —�?必须每帧固定，用 `Math.random()` 的话会变成一片沙沙乱跳的噪点，比不做还糟�?
### 成虫体表材质

成虫统一成野生型黑腹果蝇那四样：**红眼 / 琥珀�?/ 深褐�?/ 透明灰翅**
（全�?`config.visual` 里，每样都配了亮面和暗面两个色）�?
| 部位 | �?| 材质是怎么做的 |
| --- | --- | --- |
| 复眼 | �?| 一�?*球面渐变**（上前方�?�?边缘压暗�? 一点高光。每只眼**单独**建渐�?—�?两只眼在 y 上差�?0.17s，共用一个的话高光会跑到两眼中间，看着像额头上有个亮点 |
| 腹部 / 胸部 | 琥珀 | 一�?*横跨身体的浅渐变**（背侧受光、腹侧压暗），再叠一层颗粒（`speckle`�?|
| 胸背 | �?| 三根很短�?*刚毛**，朝后上方倒伏。真实果蝇这一块特别显眼，�?23px 的胸部才 10px 宽，画长一点就变刺�?|
| �?| 深褐 | 纯线条（见上面「关于描边」） |
| �?| 透明�?| 沿翅长的一道渐�?—�?**翅根厚、翅尖薄**，靠透明度差做出来；再叠一条主纵脉 + 两条横脉 |

三个不那么显然的决定�?
1. **渐变用的是翅膀 / 身体的局部坐标，不是屏幕坐标�?* 果蝇一直在转，
   跟着屏幕打光的话光会从四面八方来，反而更像一张纸片�?2. **翅膀要选「中灰」，不能选浅灰�?* 浅灰在浅色壁纸上会糊成一块看不出来的白斑�?   在深色桌面上又太亮；中灰两头都立得住。这一条是照着浅色桌面实测出来的�?3. **翅脉必须裁进翅膀的椭圆里**（`drawWingVeins` 里先 `clip()`）�?   翅脉是翅�?*内部**的纹路，出了头就变成给翅膀描边�?—�?而描边是这一整套美术风格
   明确要避开的（见上一节）。它的线宽定在屏幕上都不�?1px�?   粗一点点就从「膜上的纹路」变成「画在翅膀上的线」�?
**两片翅膀是镜像，不是同一片转个角度�?* 翅脉不是对称图形，所以画之前要先
`ctx.scale(1, side)` 沿翅膀自己的长轴翻一�?—�?不翻的话两边翅脉会朝同一边弓�?远看像两片叶子朝一个方向倒。椭圆本身关于长轴对称，这一翻不会改变翅膀的轮廓�?
**体长不到 13px 时（`drawFly` 里的 `detailed`）这一整套材质全部跳过**�?那个尺寸下复眼才 1px 出头、翅膀长约 5px，渐变和翅脉都是亚像素的，画了只是浪费�?所以刚羽化的小果蝇看着就是一只干净的小虫，长大之后才慢慢「长出」质感�?
### 「蛹是半透明的」到底是怎么回事 �?
这个反馈来过两次，两次量下来蛹都�?*完全不透明�?*�?中心像素 alpha 255，沿中心横线扫一整排也找不到一个半透明像素
（`--selftest` 里就有这条断言，失败会直接报出来）�?
真正的原因不�?alpha，是**对比�?*�?
- 刚化蛹的壳色是米白。米白的明度和浅色壁纸（白、浅灰、天空蓝）几乎一样，
  一个纯平的色块在这种背景上没有边界、没有厚度，
  眼睛只能把它读成「一层盖在桌面上的半透明东西」�?- 加了颗粒和很淡的轮廓也没�?—�?0.30 的半径在浅色底上自己就先糊掉了�?
所以解法是给壳**厚度暗示**，而不是动透明度：

| 做法 | 在哪 |
| --- | --- |
| 刚化蛹的底色单独一档，偏暖、压蓝通道，靠色相从浅色底里拉出来 | `visual.pupaColorFresh` |
| 壳内侧一圈由内向外压暗的径向渐变（不是描边，是「弧度」） | `visual.pupaEdgeShade` |
| 轮廓加深到能看见 | `visual.pupaRim` |
| 颗粒加深一档，壳面不再是贴�?| `visual.pupaSpeckle` |

**下次再有人说「蛹是透明的」，先量 alpha，量完大概率还是 255 —�?那就该去调对比度，别去调透明度�?*

挥拍圈、工具光标、抹布拖尾是 UI 特效，也不在此列�?
**罐中飞行是「和外面同一套节奏、速度小一个数量级」�?*
`updateJarred()` 复用�?`_fly()` 的写法（指数趋近目标速度 + �?悬停交替），
只是参数全部换成�?`jar.*` 名下的小值。反射的�?*速度分量**而不是把角度掉头 180° —�?后者会让果蝇沿原路弹回去，轨迹看着像弹珠；反弹后还要同步一�?`aim`�?否则指数趋近会立刻把速度拉回原来那个朝墙的方向，果蝇就贴着玻璃一直抖�?
### 关于存档

关掉程序不会从头再来。存档是一�?JSON，放在：

```
C:\Users\<�?\AppData\Roaming\fruit-fly-pet\save.json     �?主存�?C:\Users\<�?\AppData\Roaming\fruit-fly-pet\save.bak      �?上一份，主存档读坏时顶上
```

（`AppData\Roaming\fruit-fly-pet` �?Electron 给这个应用划的专属目录，取自
package.json �?`name`。卸载重装、覆盖安装都不会动它。）

**几个刻意的取舍：**

- **存的是生态，不是「你怎么看�?*。`paused` �?`timeScale` 不存�?—�?否则读档会连
  「暂停」一起继承，打开程序就是停住的；而且工具栏那两个按钮的初始样式写死在 HTML 里，
  真按存档恢复状态反而容易和按钮显示对不上�?- **粒子、挥拍特效、抹布拖尾不�?*。它们寿命都不到 1 秒，存下来再读出来也早该消失了�?- **`Fly.bait` / `Larva.eating` 不存**。它们指向别的实体对象，而且每帧都会重算�?  真正决定「跳过哪些字段」的�?`entities.js` 里的 `REF_FIELDS`�?- **写盘是原子的**（先�?`.tmp` �?rename），所以不会出现「写到一半断电、读出来半个 JSON」�?  旧存档会先留一�?`.bak`�?- **退出前会拦一�?*：主进程手里没有世界状态，得反过来问渲染进程要。等不到回复就超时放�?—�?  绝不能因为存档卡住就让程序关不掉。所有退出路径（退出按�?/ Ctrl+Shift+Q / Alt+F4 /
  任务栏关闭）都走这同一个握手�?
### 关于「对称方向飞出�?
实现的是关于屏幕中心点的**点对�?*：从左上角飞出去，会从右下角冒出来，速度和方向原样保留�?也就�?`(x, y) �?(W �?x, H �?y)`。如果你想要的是「从对面同样的高度飞出来」（镜像），
�?`world.js` 里的 `wrapFly()` 即可�?
### 关于「接近现实速度�?
真果蝇巡航约 2 m/s，但**这个数字没法直接�?*：按屏幕�?23px 对应真实体长 3mm 折算�?真速相当于一万多 px/s，屏幕上只会剩一道看不见的残影�?
所以取的是「观感上够快、够难拍」这一档：**300�?700 px/s**�?横穿 1920px 屏幕�?1.1�?.4 秒�?*难拍的关键其实不只是速度，更是变向频�?* —�?「窜」的间隔压缩�?0.08�?.45 秒之后，一挥拍空的概率才真正上来�?
### 关于「飞一会、走一会�?
真实果蝇�?*弱飞行�?*：大部分时间落在地面上爬，只有受惊或换地方时才飞�?所以状态机里加�?`walk` —�?爬行速度只有飞行�?1/35，这个落差本�?就是「像活的」最主要的来源，比单纯调快飞行速度有效得多�?
爬行时位�?*不是连续插值的**：每 78ms 整块跳一步，中间那些帧完全不�?（实�?81% 的帧位置完全静止）。昆虫迈步本来就是一步一步的�?连续滑行反而像贴图在平�?—�?这就是「一帧一帧」那种定格感的来源�?腿也跟着做三角步态（前左/中右/后左 一组，另一组同时抬起），且直接绑在步数上，
所以腿也是一格一格地换�?
落地时翅膀会后收贴住身体并完全静止，这是「在空中」和「在地上」最直接的信号�?
> 顺带解决了一个隐患：提速到 1100 px/s 时，飞行的「朝食物偏转」本来会再次失效
> �?20px 的嗅觉半�?0.38 秒就穿出去了）。但因为有了爬行模式 —�?> 闻到味道的果蝇会**直接落下来走过去** —�?食物对果蝇的聚集效果反而比之前更强�?> 这是涌现出来的结果，不是特判�?
> 另外两个值必须跟着飞行速度一起看�?*交配扫描间隔**�?0ms，太快会漏掉擦肩而过的配对）
> �?**招蝇嗅觉半径**�?20px）。都�?`config.js` 里�?
### 关于幼虫重叠

**做过一次「互相让开」，又按要求撤回了�?* 记在这儿免得以后重复踩：

当时的做法是——两条幼虫近�?10px 以内就把朝向掰开一点（不是排斥力场�?位置照旧�?`_advance` 按朝向推，所以看起来是「拐个小弯走开」）�?实测重叠率（48 只挤在一小片�?90 秒，量「多少幼虫·帧和别人挤�?6px 以内」）
�?64% 降到 1%�?
撤回了，所以现在幼�?*是允许叠在一起的**。如果哪天想加回来，
有两个坑当时已经踩明白了�?
- 别做�?*排斥力场**。那会让一群幼虫互相弹开、像磁铁同极�?  而它们本来就是要聚在一起的。目标只是「不要完全叠住」，不是「保持社交距离�?- 别让它作用于**趴在果子上进�?*的幼虫。果子本来就只有十几像素宽，
  一群幼虫挤在上面是正常的；让它们互相让开的结果是把彼此推�?`munchRadius` —�?  都站在果子外面、`eating` 大面积变 false，一份果子要多啃三成的时�?
### �?一条差点混过去的单位错误：sniffTimer

`Larva.sniffTimer` �?*毫秒**（和这个项目里所有时长一样），但曾经�?`-= dt`（秒）去减�?于是�?.6 秒闻一次」变成了�?600 秒闻一次」—�?*幼虫一生只闻到一次味�?*�?
症状极具迷惑性：看起来只是「有的幼虫找得到食物、有的找不到」，很像随机性，
实际�?*只有初值恰好接�?0 的那几只**闻到过。不崩、不报错�?只是让整套觅食机制静悄悄地废掉一大半 —�?同一份代码连跑五次，
趴在果子上的峰值是 1 / 1 / 11 / 12 / 4�?
现在 `simulate.js` 的食物验证开头有一条专门的断言守它：起�?3 秒后�?12 只里至少 10 只必须已经「闻到」（它们这时都在各自的嗅觉半径之内）�?单位错了这条会当场炸，而别的断言全都看不出来�?
### 抹布：为什么必须来回滑�?�?
**症状**：按住鼠标停在污渍上不动，它就自己消失了 —�?那不叫擦�?
**做法**：擦的进度计的是**滑过的路�?*（像素），不是按住的时间，也不是「几下」�?
```
Remains.wipe(scrub)      scrub = 这一次滑过了多少像素
   clean += scrub / scrubNeeded     scrubNeeded 由腐烂程度插值：新鲜 70px �?烂�?300px
```

于是「按住不�?= 路程�?0 = 一点都擦不掉」是**自然结果**，不需要另写一条规则去卡�?而「必须来回」也不用�?—�?它是几何决定的：`wipeRadius` 只有 16px�?**直着划过去一趟在半径内顶多走 32px**，所以新鲜的要来回两三下、烂透的要十几下�?实测：新鲜污�?72px（约 2.3 个来回），烂透的尸体 304px（约 9.5 个来回）�?
路程�?`ui.js` �?`_onMove` 里累加，而不是每帧取一次位置差�?
> �?一帧里鼠标可能移动了好几次（mousemove 的频率和帧率无关）�?> 取位置差会把中间的路程全丢掉，变成「只有慢慢挪才擦得动」—�?> 这个 bug 在快速来回擦的时候最明显，而快速来回恰恰是这个机制想要的用法�?>
> 松开鼠标时会把这半下攒的路程丢掉。留着的话，下一次按住的第一�?> 会一次性结算上一笔，表现就是「点一下就擦掉一大块」�?
**蛹壳也能擦了�?* 早先的分工是「残留物归抹布、蛹壳归手套 + 垃圾桶」，
现在两条路都留着 —�?那条分工只是让玩家多切一次工具，没有带来任何取舍�?壳需要的路程�?*固定**的（420px，比烂尸体还多）：它不会腐烂，放多久都是同一枚壳�?
> 「按住不动擦不掉」是这个机制里唯一会让人以为抹布坏了的地方�?> 所以按�?0.7 秒还没滑动过会闪一次提示条。做成常驻的一行字不划�?—�?> 面板本来就窄，为一条一次性提示占一行不值得�?
### �?产卵为什么靠屏幕边缘

**这是一条产品要求，不是生态模拟上的取�?*：这是个桌宠�?屏幕正中间是玩家干活的地方，卵和幼虫堆在那儿会挡事�?所以中间留出一块空地，卵尽量产在它外面的那一圈里�?
判据是一个以屏幕中心为心的椭圆，�?*归一�?*坐标（除以屏幕尺寸）�?所以换分辨率不变形 —�?1080p 上量的比例，4K 上一样成立：

```
centerClearRx: 0.345   // 椭圆横半�?÷ 屏幕�?centerClearRy: 0.357   // 椭圆纵半�?÷ 屏幕�?```

这两个数是对着一�?1740×980 的截图量的（红圈大约横跨屏幕宽的 69%、竖�?71%）�?想留大一�?/ 小一点，**只改这两个数就行**，别的都不用动�?
`Fly._siteOutwardness()` 把任意一点换算成「归一化椭圆半径」：
0 = 正中心，1 = 留白边界。打分就�?`clamp((e-1)/(edgeFull-1)) × edgeWeight`�?
> `edgeFull: 1.7` �?*封顶**用的。不封的话角上的归一化半径能�?4�?> 角落永远分最高，卵会在四个角堆成四堆 —�?那是另一种难看�?
**「大部分在边缘就行，并非完全强制�?* 是靠 `scatterChance`�?0%）实现的�?这个概率下她偷懒，就地开产、不挑地方。所以场上仍然会有少数卵
落在中间 —�?那是有意的，不是没生效�?
实测（模拟器每次都会打印）：

```
产卵点靠不靠边：100% 落在中间留白之外，归一化半�?中位 1.54 / 平均 1.54
实际产下的卵�?0 颗里�?30 颗（100%）在中间留白之外（含 20% 就地开产的�?```

> 回归测试同时�?*两个�?*：`_pickLaySite` 的挑选结果（上面那条），
> �?*实际产下的每一颗卵**的落点（下面那条，把 `scatterChance` 也算进去）�?> 只守前者的话，「就地开产」那条路径坏掉不会被发现�?
### 关于「窝�?
一次产�?= 一窝，有一�?*窝号**（`world.nextClutch()` 发的）。这个号从产下那一�?一路跟着：卵带上�?�?孵出来的幼虫继承它。三件事都挂在它上面�?
**�?母体先挑地方，再产卵**

配对成功不再就地开产，而是 `beginClutch()` �?挑点 �?飞过�?�?`startLaying()`�?挑法�?*在整块屏幕上**�?14 个候选点各打一个分，取最高的（`Fly._siteScore`）：

| �?| 权重 | 为什么是�?|
| --- | --- | --- |
| **靠屏幕边�?* | 0~2.6（主导项�?| 见下 —�?这是**产品要求**，不是生态取�?|
| 离尸�?/ 污渍 | 最多扣 1.2 | 那儿刚死过东�?—�?这是「危险」在屏幕上唯一的可视代�?|
| 挤不�?| 最多扣 2.1 | 别压在别人的卵堆和幼虫堆�?|

实测：挑出来的点平均离最近尸�?**690px**，全屏随机撒点只�?**376px**�?
> �?候选点�?*撒满全屏**的，不是撒在母体附近�?> 早先撒在她周�?260px 的一个圆�?—�?那在「靠边」这条规则下会直接失效：
> 她要是停在屏幕中间，260px 之内**一个够靠边的点都没�?*，所有候选分数全�?0�?> 等于没挑，卵照样落在正中间。撒满全屏就没有这个死角
> （她多飞一段而已�?700px/s 也就一秒）�?
> �?后两�?*必须封顶**。它们是在「靠边」定下大局之后做微调的�?> 不封顶时总和能到十几分、盖�?`edgeWeight`：屏幕边上一堆尸体的时候，
> 卵又被推回屏幕正中间 —�?恰好是这条规则要避免的事�?
> �?**进场减速（`siteBrakeSafety`）不是可选项�?*
> 她巡�?300~1700px/s，�?`_lay` �?*靠阻力自然滑�?*的（`settleDrag`，刻意不硬刹 —�?> 硬刹看着像被按了暂停键），滑停距离就�?`v/k`�?700px/s 要滑 378px�?> 所以不减速的话，她在离目�?26px 处「落地」，实际停在 400px 开�?—�?> **挑好的那个点上一颗卵都没有，整套选址白做**�?> 接近时把目标速度压到 `k×d×0.7`，她就会自然减速、正好停住（实测落卵中心�?12~14px）�?
**�?同一窝的卵差不多同时�?*

一窝共�?`layHatch`（产卵那一刻抽一次），每颗只�?±`clutchJitter`�? 秒）�?改写前是每颗各自 `rand(hatchMin, hatchMax)`，一窝能从第 3 分钟稀稀拉拉孵到�?14 分钟�?抖动**不能�?0** —�?那样一窝会同一帧全孵出来，像爆开一样�?
**�?同窝的幼虫更容易组队徘徊**

`Larva.buddy` 指向「一起爬的那个伴」，每隔 2~7 秒重挑一次。挑选规则就是这条需求的原文�?
```
旁边有同窝的 �?�?kinChance(0.75) 跟它�?没跟�?/ 旁边没同窝的 �?再按 otherChance(0.35) 考虑别的幼虫
两个都没抽中 �?自己�?```

两个概率**都不�?1**，所以「同窝的也可能各走各的、不同窝的也可能凑一块」�?实测 48 只（两窝�?24，全塞在一小片里）�?90 秒：结伴的里�?**85~91% 是同窝的**�?而且跨窝组合每次都会出现�?
跟的时候瞄准的是同�?*前方 26px** 那个点，不是它本�?—�?盯着本人的话两条虫会
越贴越近最后叠成一坨。另�?`l.buddy` 这一条同时挡掉了「跟已经有伴的」和
「互相跟随」，所以不会串成一列首尾相接的火车�?
> `buddy` �?*引用**，和 `Fly.bait` 一样进�?`REF_FIELDS`，不进存�?—�?> 它每隔几秒重挑一次，丢了也只是「这一轮没伴」�?
### 幼虫的嗅觉：为什么是「间歇采样」而不是每帧跟�?�?
**症状**：把苹果屑拖到别处，半径内所有幼虫在**同一�?*一起掉头，像一群被同一根线
牵着的木偶。原因是每帧都在�?`nearestFood()`，然后把这帧的食物位置直接喂给转向公�?—�?食物一动，全场的答案同一帧就更新了�?
**做法**：每只幼虫按自己的节奏隔一阵子「闻」一次，把当时闻到的位置记进
`Larva.scentSpot`（一�?`{x, y, r}`�?*不是** Food 的引�?—�?存引用会被通用
`snapshot()` 顺着写进存档，见 `REF_FIELDS` 那段）。两次之间它�?*记住的位�?*爬�?再加上每只自己的灵敏度（`scentGain`，半�?× 0.65~1.35），于是�?
- 食物被拖走后，幼虫会先把**老地�?*爬完 �?到了发现什么都没有 �?忘掉、愣一会儿
  �?各自在不同的时刻重新闻到 �?各自掉头
- 反应时间自然散开（`simulate.js` 实测 12 只：最�?0.1 秒、最�?1~5 秒，
  **没有一�?*在头两帧就转�?
两个细节不能漏：

| 细节 | 漏了会怎样 |
| --- | --- |
| 「已经趴在果子上」用**实时**�?food 判定，不�?`scentSpot` | 吃到东西是身体挨着才算的，记忆里的坐标不构成「吃到了�?|
| 爬到 `scentSpot` 却什么都没吃�?�?清掉�?| 否则幼虫会站在空地上反复朝脚底下那个点转向，看着像原地抽�?|

> 「拖动食物时幼虫反应太齐」这条反馈的回归测试就在 `simulate.js` �?*食物验证**里：
> 食物从正�?100px 跳到正北 100px，量 12 只幼虫各自的掉头时刻�?> 反应时间�?< 0.4 秒、或者超过四成在头两帧就转，都会直接报错�?> �?`food.sniffMin/Max` 的时候它会兜住你�?
### 关于空壳的塌�?
羽化之后留下的是**空壳**，玩家得一眼认出来 —�?否则会以为那还是一只蛹�?然后纳闷它为什么永远不动�?
三条线索，从强到弱：

| 线索 | 强在�?|
| --- | --- |
| **形状是瘪�?* | 活蛹是饱满的椭圆，壳上凹进去几块�?*不用比对就看得出�?* |
| 颜色比活蛹浅得多 | 得把两样东西摆在一起比一比才认得�?|
| 轮廓上留一个缺�?| 那是成虫钻出来的洞。太小，经常被忽�?|

最初只有后两条，结果收到的反馈是「米白色的蛹怎么是透明的」—�?说的其实是空壳�?*形状才是管用的那条�?*

塌陷是每一枚壳在构造时随机的（`Shell.dents`），位置 / 深浅 / 宽窄都不同，
所以同屏几枚壳形状各不相同。三个约束值得记一下：

- **深度 0.16 ~ 0.34**（相对半径的比例）。壳在屏幕上只有 28×9 px 左右�?  这个数不能小 —�?0.08 换算过去只有 1px 的凹陷，等于没凹
- **最�?2 �?*，而且两处之间强制隔开 `minSep`�?.2 弧度）�?  �?没有这条限制的话，两处坑随机到同一个方向上�?*叠加**（深度相加能�?0.68），
  壳上直接缺一块，看着像被咬掉的而不是瘪的�?  「强度随机」可以，但叠出来的强度不受控就不行了
- `Shell.radiusAt()` 末尾钳到 0.55，是**保险�?*而不是主要手�?—�?  万一 minSep 被改小、或者以后把处数调多了，壳也不会被掐成一条缝

几何（`radii` / `radiusAt` / `outline`）全�?`Shell` 上，不在 `render.js` 里：
一�?`simulate.js` 要拿它断言「壳真的瘪了」，二来它纯粹是几何，和画布没关系�?`outline()` 的每个点都带着自己的角�?`t`，`render.js` 靠它挑出「羽化缺口�?那一�?—�?缺口用的�?*同一条塌陷轮�?*上的一段，不是另画一个正椭圆�?否则缺口两端会和瘪掉的壳体对不上，接缝处露出一小截毛刺�?
### 关于倍速与时停

#### 时停和倍速是**同一根轴的两�?*

面板上那一组从左到右就是「世界推进得多快」：

```
时停 �?1× �?2× �?5× �?10×
```

时停和倍�?*共用一个入�?*（`ui.setSpeed()`�? 表示时停），不是各占一个按钮�?分成两个的原因看起来更直白，但会出现�?*暂停着、同时倍速按钮还亮着 5×**�?这种自相矛盾的状�?—�?而且是必然会出现的，因为 `paused` �?`timeScale`
是两个独立字段，两个入口各管一个，就没人负责让它们互相一致�?
两条实现上的选择�?
- **时停只翻�?`world.paused`，不�?`world.timeScale`�?*
  于是从时停里出来时还能回到原来那一�?—�?�?5× 下按空格，再按一次仍�?5×�?  而不是每次都掉回 1×
- **`Space` 走的是同一�?`ui.togglePause()`**，所以按钮和快捷键永远不会打�?
`_syncSpeedUI()` 单独抽了出来，因为改时间状态有**三个**入口（点档位、按空格�?以后可能的读档恢复）。每个都手写一遍「更新按钮」迟早漏掉一个，
表现就是「按钮显示的和实际跑的对不上」，很难注意到�?
#### 倍速本�?
果蝇一个世�?25 分钟（卵 3�?4 �?�?幼虫 20 �?�?�?5 分）�?正常速度下想看完一轮得坐半小时。所以倍速是**观察工具**，不是玩法：

| 档位 | 走完一代大�?|
| --- | --- |
| 1× | 25 分钟 |
| 2× | 12 分半 |
| 5× | 5 分钟 |
| 10× | 2 分半 |

**实现上只改一个数**：`world.timeScale`。物理一行没�?—�?`world.update()` 本来就是**分步积分**的（每步最�?1/30 秒）�?`timeScale` 只是决定这一帧要跑几步。所�?10× 不会让果蝇瞬移�?也不会穿过罐壁，只是每帧多跑几步而已。这条性质是白送的�?但如果哪天有人把分步积分改成「一帧一步」，倍速就会立刻变成穿模制造机�?
**时停时世界完全不动，但别的东西照�?*：鼠标穿透判定、工具、拖动、存�?都走各自的路径，不受 `paused` 影响。所以时停下照样能戴手套收拾残局�?摆罐子、投�?—�?停下来仔细看一只果蝇，正是这个功能存在的理由�?
**两个都不存档�?* 这是「你现在想怎么看」而不是生态本身的状�?（见 `world.serialize()` 上面那段注释）。否则读档会连「暂停」一起继承，
打开程序就是停住的，很困惑�?
> 标题行在**时停或加�?*时都会变琥珀色。这不是装饰 —�?> 「忘了自己还开着 10×」和「忘了它还停着（以为是程序卡死了）�?> 是这套东西最容易踩的两个坑�?
### 关于面板上的折叠�?
工具和倍速都用同一�?`.fold` / `.fold-toggle` / `.fold-body`（`style.css`）�?默认收起，点标题行展开�?*右侧始终显示当前状�?*（拿着哪个工具 / 几倍速或时停），
所以收起来也不会「看不出现在是什么档」�?
两者都从按钮自己的文字取状态，不另维护一份「�?�?中文名」的对照�?—�?那样加一个档位就得记着改两处（`ui.setTool` / `ui.setSpeed`）�?
### 关于「没有阴影�?
所有生命体和食物都**没有投影**。投影会让它们看起来浮在屏幕上方�?卡通风主要就来自那里；去掉之后才像是钻进了屏幕里、和桌面处在同一层�?
水果也同理，画的是「掉在桌上的一点点碎屑」——一小块苹果屑、一片香蕉皮、单颗葡萄，
而不是完整水果。完整水果摆在屏幕上既占地方，视觉上也会盖过果蝇本身�?
卵和蛆的颜色刻意朝相反方向拉开�?*卵偏米黄**（像有壳的颗粒）�?*蛆偏冷白**
（像半透明的湿虫）。只把卵调黄是不够的，因为蛆本来就是米白，那样反而更像�?
### 关于幼虫的体型遗�?
体型性状（长�?+ 胖瘦）是**顺着母体遗传�?*：一只母体产下的整批幼虫共享同一套性状�?所以屏幕上会出现「这一拨偏胖、那一拨偏细长」的效果�?
遗传用的是带**向均值回�?*的版本，而不是简单的「子�?= 母体 + 抖动」：

```
子代 = 群体均�?+ (母体 �?群体均�? × 遗传�?+ 抖动
```

那个回归项是必需的，不是调味的。如果直接照搬母体，性状就是个无约束的随机游走，
挂机跑几十代之后会漂到区间边�?—�?整屏幼虫全变成同一种极端体型，多样性反而没了�?回归项让均值成为稳定不动点。`sim` 里有对应测试：从区间上界起步�?200 代，
均值应当回落到群体均值附近（实测 0.505 vs 0.500）�?
体长倍率的区间取�?`0.82 ~ 1.18`�?*均值为 1.0**�?所以虽然每只都不一样，整体上仍然贴着�?0 分钟 20px」的原设定�?
### 两处我做了发挥的地方

1. **20�?5 分钟的蛹�?*：你的设定里幼虫 20 分钟长到 20px�?5 分钟变成虫，中间这一段没有定义�?   我把它做成了不吃不动的蛹期——既填上了空档，也更贴近真实果蝇�?   不想要的话把 `CONFIG.larva.pupateAt` 设为 `null`，幼虫会一直爬�?25 分钟直接羽化�?2. **蛆变苍蝇时会「变小�?*：蛆�?25 分钟时是 20px，而刚羽化的成虫按设定�?7px�?   这是设定本身的不连续（真实果蝇的蛹和成虫形态本来也差很多），我按原样实现了�?   想让它平滑的话，�?`CONFIG.adult.sizeBirth` 改成 20 就行�?   �?顺带一提，`adult.sizeBirth` 也压着体表材质的门槛（`drawFly` 里的 `detailed`�?3px）—�?   改小它等于让果蝇有更长一段时间是「没有材质的干净小虫」�?
## 调参

所有数值都�?`renderer/src/config.js`，按 `adult / mating / egg / larva / swarm / remains / tools / world / visual` 分组�?每一行都有注释。几个最常用的：

```js
CONFIG.world.initialFemales = 2   // 开局几只雌�?CONFIG.adult.lifespanMax     = 30 * MIN   // 想快点看到死亡就调小
CONFIG.tools.swatRadius      = 28  // 拍子范围
CONFIG.remains.wipeRotten    = 5   // 烂透的尸体要擦几下
CONFIG.swarm.enabled         = false  // 不想要幼虫集�?```

运行中也能在 DevTools（`Ctrl+Shift+I`）里直接改：

```js
__pet.world.timeScale = 8      // 临时开 8 倍速看完整循环
__pet.world.spawnBatch(10)     // 手动投放（参数是只数，不传就是配置里那个默认值）
__pet.world.swat(600, 400)     // 用代码拍一�?__pet.config                   // 所有可调数值都在这棵树上，改完立刻生效
```

## 目录

```
main.js              Electron 主进程：透明全屏窗口、置顶、鼠标穿透、全局快捷�?preload.js           contextBridge，暴露面只有窗口控制
renderer/
  index.html         画布 + 工具�?  style.css          工具栏样式（html/body 必须透明�?  src/
    config.js        所有可调参数（�?jar.* 玻璃罐、tools.netRadius 捕虫网）
    utils.js         数学工具（角度插值、稳定伪随机�?    entities.js      Fly / Egg / Larva / Food / Jar / Remains / Particle
    world.js         生命周期裁决、集群、交配、觅食、工�?API、统计、经济结�?    assets/
      alipay-qr.png  捐款二维码（真实收款码，别删，也别提交到公开仓库�?    market.js        纯计算：稀有度抽取、体重曲线、售价、价值分档、金额格式、商品链查找
    mutations.js     纯遗传：四种突变的定义、孟德尔式遗传、价�?/ 体重倍率
    render.js        全部 Canvas 绘制（含放大镜的高亮环）
    ui.js            工具栏、输入、穿透调�?    save.js          存档：启动时的「继�?/ 重新开始」、自动存�?    app.js           入口与主循环
tools/
  simulate.js        无头模拟器（bun run sim�?  probe-fly.js       单只果蝇行为探针（bun run probe�?  check-syntax.js    语法检查（bun run check�?```

### 存储布局：两个重目录�?D �?�?
C 盘（100 GB）只�?2 GB 出头，所以占地方的两�?*实体放在 D �?*，原位只留目录联接（junction）：

```
fruit-fly-pet/
  renderer/ tools/ build/ *.js    �?真正的源码，403 KB，留�?C �?  node_modules/   ──联接──�? D:\ai game\fruit-fly-pet\node_modules   (447 MB)
  dist/           ──联接──�? D:\ai game\fruit-fly-pet\dist           (581 MB)
```

项目路径没变，所�?Cherry Studio 工作区、`bun run sim`、`bun run dist` 全部照旧�?**实测�?*：electron-builder 只清�?`dist\win-unpacked` 子目录，不会�?`dist` 这个联接本身�?重新打包依然写进 D 盘、不�?C 盘�?
**两条务必记住�?*

1. **不要�?`Remove-Item -Recurse` 删这里的 `node_modules` �?`dist`�?*
   PowerShell 5.1 对目录联接会穿透进去，�?D 盘的真实内容一并删掉�?   要解除联接用 `cmd /c rmdir "路径"` —�?它只删联接，不碰目标�?
2. `bun install` 在依赖已满足时是幂等的（实测 "no changes"，联接不受影响）�?   但若它做�?*干净重装**，`node_modules` 可能被重建为 C 盘上的真实目录�?   发现 C 盘突然少�?400 �?MB，先确认一下：

   ```powershell
   Get-Item .\node_modules | Select-Object Name, LinkType, Target
   ```

   `LinkType` 不是 `Junction` 就说明被换掉了，重做一次搬移即可�?
### 两个设计上的分界

- **实体只管「怎么动」，世界管「怎么生怎么死�?*。实体的 `update()` 永远不会销毁自己，
  所有生命周期转折都�?`world._resolveLifecycles()` 里统一裁决。这样统计数字只有一个来源，不会算重或算漏�?- **穿透由 UI 层决�?*。渲染进程只上报「我现在需不需要鼠标」，主进程根�?  `clickThroughEnabled && !interactive` 决定要不要让事件穿过去。全局快捷键改状态时主进程会反向通知 UI 同步按钮�?
## 日常更新流程

**别改一行就打包一�?* —�?打包�?5�? 分钟，绝大部分时间会浪费在等 7z 压缩上�?正确节奏是：改代�?�?`bun start` 里秒级看效果 �?满意了再打包�?
### 1. 该改哪个文件

| 想改什�?| 改哪�?|
| --- | --- |
| 数值手感：寿命、飞行速度、一次产几颗卵、腐烂快慢、食物耐吃程度 | `renderer/src/config.js` |
| 外观：颜色、形状、粒�?| `renderer/src/render.js` + `config.js` �?`visual` �?|
| 行为：怎么飞、怎么找食物、怎么交配、怎么蠕动 | `renderer/src/entities.js` |
| 规则：生死判定、繁殖、集群、进食结算、统计、出�?/ 商店 / 投放结算、食物投放区 | `renderer/src/world.js` |
| **经济算式：稀有度抽取、体重曲线、售价、价值分档、金额格式、投放单�?* | `renderer/src/market.js`（纯函数，`tools/simulate.js` 直接测它�?|
| 工具栏、按钮、鼠标穿透逻辑 | `renderer/src/ui.js` / `index.html` / `style.css` |
| 窗口、置顶、全局快捷�?| `main.js` |

`config.js` 里每一行都有注释，九成的手感调整都不用碰别的文件�?
### 2. 本地看效�?
```bash
bun start
```

改完直接�?**`Ctrl+Shift+R`** 重载渲染进程就能看到，不用重启、更不用打包�?（这个快捷键**只在 `bun start` 下生�?*；打包后�?exe 里按它没反应 —�?否则用户误按一下，养了半天的果蝇就全没了。`Ctrl+Shift+I` 开 DevTools 则是打包后也保留的。）

控制台里 `__pet.world` 可以直接改参数试�?
```js
__pet.world.timeScale = 8        // 临时 8 倍�?__pet.world.dropFoods('gold', 3) // 立刻�?3 个金苹果
__pet.world.foods[0].age = 999999   // 让果子秒烂（rot 是从 age 推的，改 age 才有用）
```

> 只有�?`main.js` / `preload.js` 才需要整个退出重�?—�?这两个跑在主进程里，重载页面不生效�?
### 3. 提交前跑一遍检�?
```bash
bun run check           # 秒级：语法检查�?*先跑这个** —�?见上面那条警�?bun run selftest        # 几秒：模块加载、preload 桥、渲染路径有没有�?bun run sim 240         # 十几秒：无头推演 4 小时生态，验证生命周期和食物机�?bun run probe           # 只在怀疑某只果蝇行为不对时�?```

这三个都不弹窗、不需要盯着看，看退出码就行�? = 通过）�?
### 4. 打包

先把 `package.json` 里的 `version` 改掉�?*产物文件名跟着它走**�?
```json
"version": "1.1.0"
```

```bash
bun run dist
```

产出 `dist/FruitFlyWorld-1.1.0.exe`。绿色版没有安装步骤，直接把新文件发出去即可�?别人也不需要先卸载旧版�?
> 目前**没有存档**：每次启动都是全新的一批果蝇，不存�?更新后进度丢�?的问题�?> 如果以后加了存档，升级时就要额外考虑数据格式兼容�?
## 打包

```bash
bun run dist
```

一次产�?*两个**东西�?
| 产物 | 是什�?|
| --- | --- |
| `dist/FruitFlyWorld-Setup-1.15.1.exe` | **安装�?*。带安装向导、开始菜单项、桌面快捷方式、卸载项（控制面板里能卸�?|
| `dist/FruitFlyWorld-1.15.1.exe` | **免安装绿色版**。单文件，双击就跑，不写注册表，拷走就能�?|

两个都用同一�?`appId`，所�?*装过一次之后再跑绿色版也能共享**（不会互相覆盖）�?发给别人时：给安装包更正式，给绿色版更省事�?
### 断网也能打包：`electronDist`

`build` 里这一行是**必须�?*，别删：

```json
"electronDist": "node_modules/electron/dist"
```

不写它的话，electron-builder 会去自己的缓存目�?（`%LOCALAPPDATA%\electron-builder\Cache\electron\`）找 `electron-v<版本>-win32-x64.zip`�?找不到就**联网下载**。这台机器是不通外网的，于是打包会�?`�?packaging platform=win32` 那一步直接挂掉，报的是：

```
�?ECONNREFUSED    failedTask=build stackTrace=RequestError: ECONNREFUSED
```

�?**这个报错很有迷惑�?*：`ECONNREFUSED` 看着像代理没开或者防火墙�?其实是「少了一个本地文件」。而且 `@electron/get` 自己那份缓存
（`%LOCALAPPDATA%\electron\Cache\`）里**明明躺着同一�?zip** —�?两个缓存目录是分开的，electron-builder 不看那一个，往里拷也没用�?
写上 `electronDist` 之后，它直接�?`node_modules/electron/dist` 那份已经解压好的�?一个字节都不用下，日志里会多一�?`using custom unpacked Electron distribution`。前提是 `node_modules` 装了
（`electron` �?devDependency，和 `electronDist` 指向的是同一个版本）�?
### 只想要其中一�?
�?`package.json` �?`win.target` 数组，删掉不要的那个即可�?
```json
"win": {
  "target": [
    { "target": "nsis",     "arch": ["x64"] },   // 安装�?    { "target": "portable", "arch": ["x64"] }    // 免安�?  ]
}
```

两个都打 �?8 分钟，只打一�?�?4 分钟 —�?装机测试阶段建议先只�?`nsis`�?
### 安装包的行为

配置�?`build.nsis` 里，现在的设定是�?
- **不需要管理员权限**（`perMachine: false`）—�?装到 `%LOCALAPPDATA%\Programs`�?  不会�?UAC，也不用公司 IT 批准
- **显示安装向导**（`oneClick: false`），可以自己选安装目�?- 桌面快捷方式 + 开始菜单项，名字都叫「果蝇世界�?- 安装完自动启动一�?- 卸载�?*保留** `%APPDATA%` 下的数据（`deleteAppDataOnUninstall: false`�?
想要「一键静默安装」就�?`oneClick` 改回 `true`�?
### 改名字的时�?�?
名字散在好几个地方，而且**有的能改、有的一改存档就没了**�?
| 位置 | 现在 | 能不能改 |
| --- | --- | --- |
| `renderer/index.html` �?`<title>` / 标题�?/ 启动选择�?| 果蝇世界 | �?随便改，只影响界�?|
| `build.productName` | `FruitFlyWorld` | �?只决�?exe 名、安装目录、快捷方�?|
| `build.nsis.shortcutName` / `uninstallDisplayName` | 果蝇世界 | �?只影响开始菜单和控制面板里显示的名字 |
| `build.artifactName`（两处） | `FruitFlyWorld-…` | �?只决定产出文件名 |
| **顶层 `name`** | `fruit-fly-pet` | �?**别改** |
| `build.appId` | `com.fruitflypet.desktop` | �?**别改** |

**为什么顶�?`name` 不能�?*：Electron �?`app.getPath('userData')` 取的�?`%APPDATA%\<app.getName()>`，�?`app.getName()` 读的正是 `package.json` �?`name`
（打包后也一�?—�?electron-builder 不会往 asar 里的 package.json 注入 `productName`�?这一点可以直接在 asar 里搜 `"productName"` 验证，结果是 0 处）�?改了 `name`，存档目录就�?`%APPDATA%\fruit-fly-pet` 变成别的，玩家会以为「存档丢了」�?
**为什�?`appId` 不能�?*：Windows 靠它认「这是不是同一个程序」�?改了之后新版本会被当成另一个应�?*并排安装**，控制面板里出现两条卸载项，
旧的那个还留在机器上�?
真要连着 `name` 一起改，就得同时做一次存档迁移：�?`%APPDATA%\fruit-fly-pet\save.json` 复制到新目录去�?
### 打不动的�?
electron-builder 会去 GitHub �?winCodeSign / nsis 这些构建工具，国内经常卡住。挂上镜像再打：

```powershell
$env:ELECTRON_MIRROR='https://npmmirror.com/mirrors/electron/'
$env:ELECTRON_CUSTOM_DIR='{{ version }}'
$env:ELECTRON_BUILDER_BINARIES_MIRROR='https://npmmirror.com/mirrors/electron-builder-binaries/'
bun run dist
```

### 程序图标

图标�?`build/icon.png`�?12×512）。electron-builder 打包时会自动把它转成
**多尺寸的 .ico**�?6/24/32/48/64/128/256），所以只需要提供这一�?PNG�?
**换成你自己的�?*

1. 准备一�?**512×512 或更大的正方�?PNG**（背景建议不透明 —�?桌面图标下面
   有没有壁纸都要看得清�?2. 覆盖 `build/icon.png`
3. `bun run dist` 重新打包

就这样，不用手动�?.ico。想验证效果可以先把 PNG 拖进文件夹看缩略图，
或者直接把窗口标题栏的图标跟它对比�?
**想改现有图标的样�?*（比如换个配色、调整构图）：改 `tools/make-icon.js` 里的
`drawIcon()` 函数 —�?那就是一段普通的 Canvas 2D 代码 —�?然后�?
```bash
bun run icon          # 重新生成 build/icon.png
```

这个脚本�?Electron 自带�?Chromium 画布来画�?*不依赖任何绘图库、也不需要联�?*�?画完直接 `Read` 或打开 `build/icon.png` 就能看到效果，不满意就再调�?
> 注意 `make-icon.js` �?CommonJS（用 `require`），�?`tools/` 下另外两个跑�?bun 上的
> ESM 脚本不一�?—�?因为它是�?Electron 主进程跑的，那边默认�?CJS 解析 `.js`�?
### 顺带一提：改版本号

`package.json` 里的 `version` 会同时决�?*两个产物的文件名**�?*安装包里的程序版�?*�?升级时改它就行，别的都不用动�?
### 性能提示

`transparent: true` 的窗口在部分 Windows 显卡驱动下会掉帧。如果发现果蝇一顿一顿的�?可以�?`main.js` 里加 `app.disableHardwareAcceleration()` 试试（代价是粒子效果会朴素一点）�?
